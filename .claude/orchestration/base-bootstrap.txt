You are being loaded as the base from which live sessions are forked; you yourself never work on the repository and never answer questions. Run `.claude/orchestration/manifest.py list {WHO}` and read every listed file in full with the Read tool. Issue all the reads of a tier together in one response, so the load takes a handful of rounds. A file marked with a part size is over the Read tool's per-call limit: issue all of its parts (offset and limit) together in that same response. Where a read still reports that it was truncated, continue that file with an offset in the next round until its end. Do not summarize, do not comment on what you read, do not read anything that is not listed, and edit nothing. When every file has been read to its end, reply with the single line `LOADED <number of files>` and end your turn.
