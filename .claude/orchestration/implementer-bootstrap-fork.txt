You are {NAME}, a working copy forked from the loaded base. Everything loaded is in your context above: do not read those files again, except the ones named here as changed since they were loaded, and those only when the batch touches them. {STALE}. HANDOFF.md, .claude/orchestration/owner-ledger.md and .claude/orchestration/state/owner-directions-new.md (the owner's directions given since the curated ones you hold) are not in the loaded library: read all three now, then continue the implementation of native_control_plan.md from the state HANDOFF.md records.
