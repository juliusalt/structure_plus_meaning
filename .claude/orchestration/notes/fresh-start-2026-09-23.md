The harness changed while the run was stopped (2026-09-22 22:37 until this start). What bears on planning:

- **Bases serve reading and steering together.**
  The owner's reference pins are retained; no use ranking, weighted value score or 530K selection ceiling chooses
  what belongs. The full library/tool catalogue and whole plan remain visible. A base holds, in the order each rests
  on what comes before it — which is also the order of how often each modifies what it holds — the owner's words and
  the practice, the reference and its reasoning, the plan (high: its map) with the decisions and the tool index, the
  plan's notions with REASONING_REUSE.md (max, xhigh), and the catalogue of theories with the tools held whole; nothing in it depends on the queue. Role reasoning sits over its
  own subject.
- **No session holds the orchestrator's own memory**: Claude Code's auto-memory is off in every session of the run,
  and a base holds, of the development's memory, only the library's working practice (the owner's words on the
  library's content and the Isabelle lessons earlier sessions recorded). The orchestrator's notes are its own; the
  run needs none of them.
- **What the bases hold was reviewed, and three things of the repository's own are for you to plan** (the harness
  holds them as they are): 25 theories have no row in THEORY_MAP.md, so the catalogue every session holds says
  nothing of what they offer — Allocated_Environment_Presentation, Artifact_Lookup_Presentation,
  Builtin_Investigation_Execution, Cached_Graft_Presentation, Certified_Cause_Presentation,
  Concurrent_History_Presentation, Concurrent_Replay_Presentation, Data_Reading_Presentation,
  Digit_Allocation_Presentation, Encoded_Environment_Presentation, Environment_Update_Presentation,
  Finite_Derived_Values, Finite_Inspected_Values, Finite_Presented_Cause_Families, Finite_Presented_Storage_Notions,
  Generation_Record_Presentation, Graft_Admission_Presentation, Graft_Presentation, Native_Replay_Presentation,
  Parallel_Inspection_Caches, Parallel_Replay_Readers, Required_Cause_Presentation, Requirement_Decision_Presentation,
  Use_Allocation_Presentation, Use_Codec_Presentation; and DECISIONS.md's `## Consolidated overnight checkpoint` is a
  state of 2026-09-18, not a decision, which every session holds by its first sentence as one.
- **Sources that are history, or that cite a source no session holds, are yours to settle** (the harness holds none
  of them). No base holds DEVELOPMENT_WORKFLOW.md or AGENTS.md any more: every requirement of the workflow document
  is the owner's words you hold (codex-owner-directions.md §6–§15, owner-directions.md) and problems.txt, carried out by
  the protocols; its one statement of its own, that Isabelle establishes the bootstrap account's adequacy, is the
  owner's open Q3. native_control_plan.md still cites it for the standing rule and Isabelle's bootstrap role, where the
  owner's words (codex-owner-directions.md §10) are the source. ADMISSION.md and proposal.txt, which nothing
  current cites, are retired by the owner's word (2026-09-23; their deletions stand uncommitted in the working tree,
  the owner's, with README.md's one link to ADMISSION.md pointed at git history). plan.md, OBLIGATIONS.md and
  GENERALIZATION_REVIEW.md stay: native_control_plan.md cites them, and it is not to absorb them — "it is the current
  plan with a current scope" (the owner, 2026-09-23). Of REASONING_REUSE.md, which the planner and the middle roles hold,
  the owner said (2026-09-23, to the harness's development): "reasoning reuse should be heavily modified as it should
  be mostly redundant due to faulty previous reasoning and generalization" — measured, 83 of its 85 commits before the
  run revised its existing sections, and the run removed 23 of them.
- **A task's own relations are given to its sessions as they start**: what the theories its brief names stand on
  (statements for a build or fix, definitions for the middle roles), those theories and what uses them (signatures),
  less what the base holds — in files its first message names for its first batch — with the other current work on
  the same theories. Name every theory a task works on in its brief (`theories/X.thy` or `X`): that is what its
  relations are found from. Their size comes out of the task's room: the brief's form check says how much.
- **Every session runs on Claude Opus 5.5**, with auto-compact off: the notice to wrap up comes at 947K, 30K below
  the 977K past which nothing is sent. At a given effort Claude Opus 5.5 thinks more per turn than Claude Opus 5, so
  size work by the room the brief form check states, not by how far past sessions went.
- **Every base runs on a delta, max too**: what changes in a base's parts after they loaded is written as a text at
  every change; a session holding the texts is made only once the base's own forks have paid for it, and a session
  that starts is given the texts it lacks. The judging roles' layers and the knowledge base hold them in their first
  message; the knowledge base takes later texts in with the planners' notes, and is built again only when max's parts
  are refreshed, or it has grown, gone cold or been lost.
- **Nothing a session holds is given it again.** The knowledge base holds your protocol and the graph, and each
  episode is given what changed in them, and in HANDOFF.md's Now and Open since the knowledge base read it; a task's
  session it comes back to is given its brief's changed lines; and a file's changed parts since what you hold can be
  read alone.
- **Your order puts its tasks first, and takes a task out of the queue only when it says so** (the owner,
  2026-09-24): every queued task your order does not name stays queued after the ones it names, as it stood, unless
  the order says the queue is its tasks alone; a finished task leaves the queue whenever an order is given. The queue
  you inherit is the last run's: say so in your first order, or every task you leave out stays queued after yours.
- **What a session is given and told.** A build's,
  fix's, design's, investigation's and brief task's session is given, in its first message, the statements of the
  facts and definitions its brief's Inputs and Decided name, as its tree holds them: name each fact a task consumes
  exactly (`Theory.name`). A change that writes a theory is told what the source checks and the import graph say of it
  (your standing last step, now the harness's at every change), what it adds that the library already has — a specific
  name, a statement word for word, a definition's body — and what its row still offers that it took out; its reviewer
  reads the same over the whole change. THEORY_MAP.md rows and ROOT lines are written by the theory's name, the
  imports column read from the theory. The commit states the harness's own record of the session's probes beside its
  check, so a Validation paragraph holds what no record does: briefs need not ask for the standing step, a probe's
  flags or runs restated. And a task need not bring main in before its hand-over for the check's sake: batches and
  trains check the work merged onto main as it stands, so a lagging tree rebuilds nothing more (HANDOFF's working rule
  came from the landing check in the task's own tree, #124, before batches and trains); it brings main in when told
  its lines meet main's. In a task's own tree the hand-over is one step — its files, the repository's
  check, its commit.md — so a brief's Acceptance may say "the repository's check" without its command, and names a
  command only for another check.
- **A task's passing state is shown, not kept.** The graph gives each open task's stage with a parked task's reason
  and since when and a rejected task's rounds, and your status the landings of the last three hours with their
  commits: HANDOFF.md names a task and what it is for, never where it stands (the planners of 09-21/22 spent 405 of
  their 908 edits of it on such words, a mirror stale between edits).
- **138 of THEORY_MAP.md's 1,812 rows name imports their theory no longer has** (the Investigation theories now
  importing their `_Base`, and others); a row written again by its theory's name alone reads its imports again from
  the theory, so one small task could set them all right — a row's imports column is what a reader of the map takes a theory to stand on.
- **Four things stand twice in HEAD** (found by replaying the restatement hints over the 68 landed changes of
  09-21/22; each named to its writer from now on): `Development_Request_Citations.predecessor_check_rule` defines what
  `Native_Collection_Programs.native_member_later` defines, and so does `native_some_rest` beside it;
  `Native_Collection_Programs.native_context_call_rule` what `Development_Verdict_Mentions.store_found_rule` does;
  `Development_Verdict_Statements.keys_term` what `Development_Rows.development_row_family` does; and
  `Factor_Indexed_Readings.reading_heads_indexed` (with its values and counted siblings) states what
  `Development_Answer_0ccf746fe2cf.indexed_heads_exact` states. Whether each pair is one notion, and so one
  consolidation of the kind the fixes from #100 on made, is yours to judge.
- **Q9's file can go now.** `tools/__pycache__/build.cpython-314.pyc` is still tracked (and rewritten in the working
  tree by every run of the tools); the finalizer stages the removal of a tracked path that .gitignore matches since
  09-21 (finalize.stage, after task 48's attempts were refused), so a task handing over its deletion commits it.
- **A reviewer corrects words itself, and a follow-up's brief can be begun for you.** A review that finds the work sound
  and only its commit message, its result or a theory's THEORY_MAP.md row misstating it corrects them and accepts,
  naming each under `## Corrected` (your commit event says "Corrected by its review: …"); about 12 of the 31
  rejections of 09-21/22 were such words, each a fix round. A reviewer writes nothing else, so a finding about a
  theory, code or a decision entry is still a rejection. A task's brief can be drafted for you from one or several
  reviews' follow-ups: the follow-ups verbatim, the review and the names they give among its Inputs, and the parts that
  are yours marked (why now, the files, what shows it done, Decided, Plan, Size), which a placement refuses while one
  is left; and a task you create or rewrite can say which task it continues.
- **The owner turned five switches on (2026-09-23, before this start).** `continue-by-fork`: a task marked as continuing
  one task's work forks the session that did that task while it is warm. `grouped-repairs`: small repairs of the same theories go into one task (your protocol says how far).
  `support-apart`: reviews have a slot of their own beside the two producers. `measure-bound`: a held measurement stops
  every other run, so it is bounded at 3 minutes (the number in `state/measure-bound`) — a brief that asks for a timing
  sizes it to fit, or asks for one measured beside everything else, which is told the machine's load while it ran. `role-layers`: each role's sessions fork a reasoning layer built for the role from what
  the run has shown of it and the work ahead in the graph — so a brief that names its inputs and what it delivers
  exactly also feeds the layer its role's sessions start from. And the review beside the check (C9): a task's review
  starts at its result, beside its check; a rejection and the check's failure reach its session in one fix round, an
  accept of work whose check then fails is void, and every re-review is the reviewer that judged it, given what changed
  since its verdict. A timing is now asked for holding the whole machine (three minutes at most) or beside
  everything else (a run like any other, told the load): a brief that asks for one says which.
- **A theory change probes itself when it says so.** An implementer's or fixer's change that writes a theory
  says whether it is probed, and the probe's finding comes back in the change's reply: a brief need not plan a probe
  as a step of its own after each change.
- **An accepted task left in review is committed by the watchdog.** Tasks 128 and 147 were accepted and checked but
  stood in review for eight hours with nothing to commit them; they are committed, and land in the next train, once
  your order releases the graph.

In flight — each session's cache is gone, so each task continues in a new session, in its own tree:

- task 44 (implement-44, interrupted as its measurement ended): 5 files changed in `.build/trees/44` — DECISIONS.md,
  ROOT, THEORY_MAP.md, `Development_Verdict_Mentions.thy`, `Native_Path_Stores.thy` — and one new,
  `Development_Verdict_Witnesses.thy`;
- task 278 (fix-278, interrupted as a measurement began): 2 files in `.build/trees/278`;
- tasks 192 and 279 (parked; their probes' outputs came back at 22:36:09 and were not read): 192 has one new theory in
  its tree (`Development_Incremental_Verdict.thy`), 279 two changed files (THEORY_MAP.md,
  `Isabelle_Entity_Export.thy`);
- task 276 (its check passed at 22:36:43; its review has not started): 8 files in `.build/trees/276`.

Trees whose tasks are not running: `.build/trees/143` (task done; 1 modified file) and `.build/trees/176` (task
deleted; one commit and a modified THEORY_MAP.md that never landed) — carry them or say why they are superseded.
`check-a`, `check-b` and `train-a` are the harness's own integration trees.


## Bases redesigned for understanding and alignment

The owner rejected use-only selection and numerical value scores. The rebuilt bases preserve the owner's reference
pins, the complete library/tool vocabulary and the whole plan. A task's own relations — what the theories its brief
names stand on, those theories and what uses them — are given to its sessions as they start, with the other current
work on the same theories; future blocked work is visible in the plan and catalogue. Source-reference relations help
discovery and are not proofs of semantic dependency or adequacy.

Shared reference reasoning precedes direction and catalogue. Reviewers, designers and task designers reason over the
current changes; implementers, fixers and investigators receive changes above their methodological reasoning or
through current reads. Every reasoning block is generated interpretation, subordinate to the source and owner intent.
Its presence is not proof that a contract was consumed correctly. Inspect both missed discovery and misuse of
material already held when reviewing the next run.

Take stock of the graph before queueing. Re-evaluate brief sizes against the actual rebuilt prefix, including role
reasoning and churn, and the task's own relations (the brief form check counts them, and the room before
them). A task whose Size with its relations exceeds its room is handed back at its first start; a fix and a task
already begun are not. Smaller coherent tasks are acceptable; do not remove required context or plan obligations to
preserve old sizes. Complete the repository's high-level workflow through its native process; the base redesign
supplies context and does not settle native-control obligations.
