# Work list, 2026-09-21 (the owner: per-chain depth, correcting the whole graph, every command batchable)

1. Per-chain depth rule
   - [x] chain_depths, past_the_limit, graph_after
   - [x] proposals, accept's re-check, the planner's TaskUpdate and `v2.py blockers`
   - [x] status line and graph text show each task's chain depth and what is past the limit
   - [x] designer and planner protocols per chain
   - [x] tests rewritten for per-chain (real chains, not a faked depth_at_start; depth_at_start removed)
2. Correcting the whole graph (tasks and dependencies)
   - [x] planner protocol: rewrite, delete, remove or re-point edges; deleting wrong work reopens room
   - [x] the rule never refuses a deletion, an edge removal or a rewrite (tested: guard, blockers, edit)
   - [x] a deleted task's dependents re-pointed in the same edit, or the edit is refused whole
3. Every command batchable (read, write, edit)
   - [x] every v2.py command: groups separated by `--`; several ids directly for drop, accept, proposal, tell
   - [x] `v2.py edit FILE`: batched graph edit, checked first, all-or-nothing; accept rebuilt on it
   - [x] the gather reads task briefs and proposals (`task:ID`, `proposal:ID[:KEY]`)
   - [x] protocols and usage state the batching, several writes or edits in one Bash call included
   - [x] found on the way: a review task the planner writes was never linked to what it reviews (link_reviews)
   - [x] found on the way: a graph edit through the harness (`edit`, `blockers`, `queue`, `drop`, `accept`) did not
         count as the planner's production, so batching spent its reading budget; it counts now
4. [x] Tests: 375 pass; two end-to-end walks (a tree task landing on moved work; a build taken back through its
   review to its commit) with no ATTENTION; mutation check made reliable (no bytecode, BROKEN reported) and its
   misses fixed at their causes; final run: 103 mutations, every one caught
5. [x] README, handoff note, the planner's waiting event replaced
6. [x] Report

7. Reading limits (the owner, later the same day): batch = one read, 30K bytes a batch, 3K a read, two tiers — done,
   tested. Filtering reads of lines already in context — done, tested, mutation-checked; the gather filters, bounds
   and records its files the same way (see the handoff's last section).
8. [x] The gather a read like any other (`v2.py read`, any time, counted and bounded as any read); the token bound and
   planned inputs deleted; every output cut to a read's bytes, checks included (their end), kept whole under
   .build/outputs/ to be read on by its lines. 405 tests, 159 mutations caught.
9. [x] Grep, Glob, WebFetch, WebSearch out of session-flags and refused to every role; bases and sessions record their
   flags, and nothing forks, pings or layers over one started with others. 409 tests, 165 mutations caught.
   **The three bases must be rebuilt before a restart** (the owner's). Open: the consultant role; Agent, TaskOutput,
   Monitor (always refused, never used) — decide before the rebuild.

## The plan, B1 to B5 (the owner: D1 leave the consultant for now; D2 tell a session to batch when it makes a single
## change; D3 checks stop at the first failure; stop after B5 — no base rebuild or seal, no commit, no restart)

10. B1 probes (small sessions outside the harness, cleaned up after)
    - [x] tool deferral is ToolSearch's doing: without it every tool loads at start (no setting needed)
    - [x] EndConversation goes with ToolSearch
    - [x] a PreToolUse `updatedInput` rewrite is what runs (the cut, the filtered read), and what PostToolUse is given
    - [x] a heredoc write through Bash passes auto mode
11. B2 one command for changing files: `v2.py change`
    - [x] the format (blocks in a quoted heredoc), checked whole, applied whole or refused whole, loud refusals
    - [x] its report, bounded; D2: a call that made one change is told to batch
    - [x] the guard: a write with exact targets (write guard, ownership, production)
    - [x] every other content write refused, pointing to it (redirect into a file, tee, sed -i, perl -i, a script writing)
12. D3 a check stops at its first failure
    - [x] first_error.py (not tools/: build.py is in all 52 recipes' closure), foreground and background,
          the whole tree ended, said; the circling rule still reads the failure
13. B3 the tool list: Bash, TaskCreate, TaskUpdate, TaskStop
    - [x] session-flags (Bash TaskCreate TaskUpdate TaskStop), the matcher; the Read-only loader paths removed
    - [x] every removed tool refused with its own pointer; the paths that served only Read, Edit, Write taken out
14. [x] B5 protocols, README, handoff; tests of every piece (431 pass); 192 mutations, every one caught; the flows
    that walk a task end to end leave no ATTENTION
15. [x] (the owner, later) every command a session makes kept, numbered; `v2.py again N` sends only the correction
    (SEARCH/REPLACE on its text), guarded and run as if typed, counted as what it runs; a fixable refusal and a command
    that ended failing are told so (not a check); a refused change exits failing
16. [x] (the owner) checks not stopped at the first error: run to the end, every error listed last with where it
    stands, to be fixed together (check_errors.py replaces first_error.py); what a session keeps — outputs, commands,
    error lists, its meter and lock — goes at release, the newest 50/50/10 while it lives
17. [x] (the owner) a pass over what the restrictions leave undoable: the owner's questions (`v2.py ledger`), native
    and other failures listed for a failed check, the finalizer's check listing its errors, a failed command's end,
    command output under .build/, own drafts readable, a fact by its lines, git's reading forms, shell comparisons and
    `&>`, a search's answer not a failure
18. [x] (the owner, last) review the bases and layers for currently incorrect information, every role's prompt, and
    the README in full; raise the limits to 5K a read and 50K a batch. Limits raised; memory's active base corrected;
    findings 1–20 in the handoff's last section are the rest — the next agent does them, then the full suite and the
    full mutation check, then reports.
    Done by the next session: findings 1, 3, 6–20; the suite on the limits (448) and on every change after; then, at
    the owner's word, an own second pass over what the bases hold and every rendered role prompt (the handoff's last
    section). Left: the full mutation check, then the owner's word on the rebuild, the commit and the restart.
