Of what you hold, these files changed after the library was loaded: {STALE}. When your work touches one, read what
changed in it rather than the file again: `.claude/orchestration/v2.py read changes:NAME` gives its changed parts in
their current form (`changes` those of all of them). {LAYERED}
