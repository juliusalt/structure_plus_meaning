# Review of the generalization machinery, 2026-09-10

The machinery has helped expose lost distinctions and verify proposed repairs.
It has not absorbed all repeated reasoning. The most persistent external steps
have been deciding which conflicting observations to withdraw, reassessing
the scope of an apparently successful experiment, choosing a construction when
the available language cannot repair a failure, and auditing the evidence that
connects a finite table to its intended subject. Successful evaluator execution
does not discharge those steps.

This review covers every accessible earlier evaluator result found in the
project artifacts: 165 executions, comprising 141 observation investigations
and 24 inference investigations. There are 157 evaluated inputs and eight
deliberate formation rejections. The [individual run ledger](validation/generalization-review-2026-09-10/RUNS.md)
and [complete finite results](validation/generalization-review-2026-09-10/historical-runs.json)
retain the per-run findings. The inventory contains 78 distinct finite
evaluation states. Repeated states often check changed implementations or
proof contexts; they are not counted as independent content discoveries.
The five completed input-admission runs captured after this inventory, and the
new experiments prompted by this review, are follow-up evidence.

## What was checked, and what remains recoverable

Every saved evaluator result was independently reconstructed from its complete
finite input. All available result fields agree: whole-input formation,
candidate profiles, every ordered loss row, residual comparisons, sound facets,
conflicts, repair witnesses, extension selections, and language obstructions.
The inference checks include full rules, repeated premise occurrences, closure,
demand, and complete reasons. An empty residual in a rejected input was not
treated as success.

Recorded engine snapshots, generated modules, runtime programs, saved cases,
proof receipts, and logs still match their recorded digests. There are 52
distinct changed external path-and-digest references, of which 49 original
versions can be recovered from preserved files or Git. Three versions remain
unavailable: an older investigation command and two permission drafts. A later
version at the same path is not substituted for missing historical bytes.

All 21 source-readiness cases with recorded accepted-build evidence have their
seed decisions checked against the exact sources in that accepted receipt.
For 18, the complete import closures and every conditional local/parent premise
also match the recovered original theory headers. The remaining three refer
to the two unavailable permission drafts, so their full import reconstruction
remains unverified. The earliest execution also records less runtime provenance
than the later command. These are limits on retrospective verification, not
evidence that the corresponding mathematical claims are false.

The [historical evidence audit](validation/generalization-review-2026-09-10/historical-evidence.json)
retains the source checks and unavailable versions. The supplementary inventory
includes failed, interrupted, and successful proof
diagnostics, native decision matrices, fixture runs, and duplicate summaries.
They have distinct evidential roles. A failed export from the first permission
experiment was explicitly excluded; the accepted replacement is the execution
counted above. Timeouts, a Java startup failure, and deliberate invalid Boolean
inputs supplied no successful semantic result. Proof-diagnostic retries show
that proof checking caught defects, but their eventual success alone does not
establish useful abstraction or complete subject coverage.

## Content findings across the accumulated runs

| Run family | What the executions established | Critical assessment |
|---|---|---|
| Initial permission readiness and later source-context investigations | Changed parent contexts invalidate reuse even when local text is unchanged; unsupported local obligations remain demanded. | This is useful conditional readiness. Imports and file presence were not treated as mathematical proof. Three historical contexts have the recovery limit described above. |
| Shared completion | Separate existential successes fail to preserve the existence of a shared witness; the joint observation settles the stated comparison. | The machinery identified a substantive compatibility distinction. Its initial report still left the next facet choice to external reasoning; later repair reports absorbed that choice. |
| Permission observations | Formation and truth each lose distinctions alone in the supplied family. The paired observation succeeds. | Early hand-supplied tables had an explicit code-link gap. Later cases compute actual program decisions and separately prove the general observation contract. Finite execution and the universal proof have different scopes. |
| Substitution and collapsed markers | Both marker observations recover the tested substitutions; collapsed markers leave failures beyond their full available language. | The report correctly identifies a language limit. Changing the marker assignment remains a construction choice requiring its own exact contract; adding a selected facet cannot solve that limit. |
| Program probes | Two marker probes fail on the two actual programs; the third separates them. | Adequacy is relative to this family. The registered operation prevents a caller's broader prose scope from replacing the actual experiment. General claims about arbitrary native programs remain separate. |
| Symbolic calls, rule schemas, and prescribed sockets | Exact binder and socket links matter even when weaker rule meanings agree. Source readiness tracks the new proof obligations. | Keeping complete subjects and explicit intrinsic links was productive. The evaluator did not itself invent or certify the required native construction. |
| Repair and extension development | Sound alternatives and their witnesses settle several earlier examples. The collapsed language remains inadequate. | This absorbed repeated addition choices, but extension necessarily retains selected conflicts. It did not yet automate the recurring withdrawal-and-reassessment step. |
| Native profile presentations | Ordered results reject equivalent reorderings and repetitions. Complete comparison succeeds. | The initial scope contained only equivalent presentations, so even the empty selection was adequate. Adding a genuinely different result exposed the missing coverage. The evaluator was correct; the original experiment was too weak to support a discriminating claim. |
| Empty-table scope admission | Row traversal misses invalid unused context fields; explicit context admission fixes the stated cases. | The result usefully directed a reusable ordinary context clause. The original smaller query domain remained legitimate; it was not relabelled as whole-investigation admission. |
| Nested observation collections | Literal outer comparison loses equivalence when an inner set has another presentation. Complete inner and outer coverage succeeds. | The construction preserves both set levels and keys. Repeating the same initial/extension/safe/empty sequence again exposed an unabsorbed revision pattern. |
| Ordered pair coverage | Diagonal pairing cannot supply every ordered pair. Complete Cartesian coverage settles the independent criterion. | The old operation's actual diagonal meaning remained intact. Complete ordered coverage required a different operation, not a more favorable observation label. |
| Complete profile and loss tables | Sparse tables omit required empty rows; complete tables retain every candidate and ordered pair. | Empty results are part of the subject. The native decision matrix checks this boundary extensively, but the content case still examines a declared finite family. |
| Complete investigation input, initial stage | Reusing scope checks for a comparison relation imposes an extra endpoint restriction absent from the original input. | The unchanged original formation condition must govern admission. The initial language cannot repair its errors; subsequent work supplies an actual separate relation presentation and admission contract. |
| Rejections, cycles, occurrence and identifier controls | Unused malformed inputs are rejected, unsupported cycles stay unresolved, premise uses stay distinct, and large natural identifiers survive execution. | These protect the trustworthiness of the evaluator. They are not additional demonstrations of semantic generalization. |

The native execution matrices add substantial counterexample coverage: profile
and loss operations, complete scope admission, nested collections, Cartesian
coverage, and complete tables. Their supplied case counts must not be added to
the 165 content investigations or treated as independent abstractions. Two
profile receipts describe the same 17,764 executed decisions; the later receipt
corrects an accepted-case count. Similarly, two Cartesian receipts reuse the
same decision matrix. The old repair matrix exhausts the two-candidate,
two-facet, one-witness universe. That universe cannot expose every interaction
between conflicting facets and newly needed repairs.

## Repeated reasoning now entering the machinery

Twenty-four well-formed historical runs select conflicting facets. In sixteen,
withdrawing the unsound selected facets exposes additional missing comparisons.
Fourteen have no adequate preserving extension although an adequate available
basis exists. The old report already exposes the whole sound available
language; the missing operation is a justified transition that conserves the
selected sound part and explains new additions after withdrawal.

The explicit counterexample uses two candidates with the intended comparison
`0 <= 1`. Selected facet 0 conflicts with that comparison but already separates
`1` from `0`. Selected facet 2 is sound and constant. Facet 1 supplies a sound
separation; unselected facet 3 is constant. The old report returns no repairs.
After facet 0 is withdrawn, a new failed pair `(1,0)` has a repair through
facet 1. Reusing the original empty repair report fails. Selecting every sound
facet works but adds facet 3 without a new-distinction witness.

[Observation_Revisions](theories/Observation_Revisions.thy) derives the general
transition from existing conflicts and repairs. It preserves selected sound
facets, excludes every selected facet that any adequate available basis must
exclude, and recomputes complete repair witnesses on the retained selection.
The resulting selection is sound and has exactly the failures of the whole
sound available language. It is adequate exactly when an available adequate
basis exists. This is a separate revision proposal; the preserving extension
keeps its original meaning. No minimum or unique selection is claimed.

The executable interface now returns retained facets, withdrawals, recomputed
witnesses, the proposed selection, and its remaining comparisons. Rejected
inputs receive no revision proposal. The first actual counterexample execution
returns retained `{2}`, withdrawn `{0}`, repair `(1,0,1,0)`, and selection
`{1,2}` with no residual.

The runner also retains the exact bytes of external case evidence and execution
tools in each new run bundle. This addresses the historical recovery gap.
Digests identify those bytes; they do not establish the semantic truth of a
case or identify equivalent presentations. A generated, proof-checked export
theory supplies the shared interface operations for each registered content
case, while the registry fixes its actual source and scope.

The actual method comparison starts with soundness as its selected observation.
It returns 19 missing comparisons and requests the remaining quality facets.
Following that report settles the stated method comparison. Independent checks
find recomputed revision to be the only supplied method satisfying every
workload condition. They also show that soundness is redundant as an observation
in this particular finite family when the other three facets are present.
The 16,384 two-candidate, two-facet, two-witness cases and 11 additional controls
all agree with independent reconstruction, including 836 cases with newly exposed
missing comparisons. The complete 758-theory main session is accepted.

The owner's subsequent correction identifies the larger missing capability:
the arguments used to construct the content must become a growing native
reasoning library consumed by development. The [reasoning reuse inventory](REASONING_REUSE.md)
records those arguments, the native constructions already present, and their
missing connection to applicability, candidate construction, and residual
reasoning. Revision of a supplied finite language does not satisfy that broader
requirement. This integration is now the central development task.

## Continuing review rule

Review content quality after every two completed content loops, and sooner
when the same reasoning is performed again or an apparently successful scope
proves too weak. A loop includes the initial evaluated question, its actual
residual and witness evidence, the resulting construction or selection, and a
new evaluation on the same intended subject. Changes of scope or observation
language are recorded explicitly.

At each review, reconstruct the new runs, inspect their subject and evidence
boundaries, and identify repeated judgments that still occur externally. State
an independent requirement for any proposed improvement, compare actual method
candidates using the existing evaluator, prove the reusable rule, then execute
the revised method and follow its returned proposal. Keep unresolved language,
scope, or evidence deficiencies visible. Track the reason each new general
rule improves produced content; elapsed time and the number of passed builds
are not the quality criterion.

The next reviews still owe structured assessment of scope adequacy, separation
of available-language limits from comparison-model limits, and guidance for
constructing observations outside the supplied language. Profile inclusion
itself can only realize a preorder; an arbitrary supplied relation can be
well-formed yet incompatible with that model. The current report does not
automatically diagnose that stronger obstruction. Native presentation of the
whole investigation report, native mathematical-proof admission, the wider
application audit, and the final every-line audit also remain open.

## Follow-up: complete native input and actual revision

The main command now executes complete native investigation-input admission.
On its eleven declared subjects, the endpoint-restricting observation leaves
56 residual comparisons and 24 conflicts. The returned revision withdraws it,
recomputes 56 repair witnesses, and selects the complete native admission
observation. `tools/follow_revision.py` consumes that actual proposal in a new
run while retaining the preceding case, receipt, and wrapper bytes as evidence.
The follow-up has no residual or conflict. Independent reconstruction agrees
with both complete finite results. Five controls reject failed or unstable
execution evidence, rejected input, a malformed selection, and changed case
bytes. The host wrapper follows the proved revision transition; it does not
certify the semantic adequacy of the declared experiment.

The native planner and learned-rule integration now have an accepted dependency
session and executed results. They include a goal-generated reconstruction of
this same input program and a conditional pairing rule consumed by schema
generation and inference. The complete 758-theory main session has also passed. The required broader decision workflow remains as stated in
[DEVELOPMENT_WORKFLOW.md](DEVELOPMENT_WORKFLOW.md).

The complete-input result exposed a mismatch in the raw JSON command as well.
It refused a natural comparison row outside the candidate set before the
proved evaluator ran, although the original formation condition retains that
row. The redundant endpoint guard has been removed. The unchanged empty-
candidate case with relation `{(2,2)}` now reports a formed input, and its
complete result agrees with independent reconstruction. The saved earlier
refusal contains no semantic result and is recorded as a host failure. The
regression checks also retain rows with either or both foreign endpoints in
nonempty candidate scopes, while still rejecting malformed natural rows.

## Follow-up: construction, premise evidence, and useful decisions

The learned rule changes the actual generated inference family: zero
applications before adding it and 27 afterward on the unchanged three-call
frontier. With one known premise missing, the original goal remains unresolved
and has three identified premise reasons. Establishing that premise settles
it. A second execution family uses new counter values. These results show
reuse of a construction argument; they do not show automatic discovery of
that argument's mathematical proof.

The formed false counter control is more discriminating than successful
well-formed examples alone. The unsafe method produces an empty residual for
a goal proved false in the native planner. It violates the separate
known-premise requirement. The native admission method retains the unresolved
condition, while still settling the valid workload. Establishing no premises
avoids this false settlement but fails the valid workload. Thus neither input
formation nor soundness by itself measures useful development.

The actual method investigation returns two failed comparisons from the
premise-soundness facet, with two repair witnesses requesting the useful-
settlement facet. Following its returned selection `{0,2}` yields no residual
or conflict. Independent reconstruction verifies both results and every
retained artifact digest. It also enumerates every available facet selection:
facet `{2}` alone suffices within this declared family. The result supports
native admission on these workloads, and no claim about all possible methods.

The thirty structural and native reasoning executions retain all applications,
source schemas, bindings, premise occurrences, residuals, demand, and reasons.
The independent verifier enumerates complete valuations and checks every
original premise rather than reproducing the executor's ordered join.
Material controls preserve all five operands; unused malformed data causes
whole-input rejection even when another rule would settle the queried goal.
The generated module and full output are retained in the repository, and a
replay using those retained bytes passes. This is one control family replayed
twice, not sixty independent content discoveries.

The generated input plan is also executed at 336, 337, 1000, and a large natural
identifier. All four installed programs give the same correct eleven
admission decisions. Starts 0 and 335 are outside the proved execution range
and return no admission decisions. The universal source-qualified contract,
the finite execution family, and proof validation remain distinct evidence.

The complete main validation passed with the original source and tool digests
unchanged. The earlier 300-second timeout is retained as a failed validation
attempt; the accepted run used the documented 900-second allowance. The
revision wrapper now captures the case and receipt bytes once each, so a
replacement between reading and execution cannot silently change either the
proposal's subject or its evidence. Both source-replacement controls retain
the original proposal and reject the changed evidence before execution.

The source-readiness investigation was rerun on the same two root theories
after their revisions and full-session acceptance. Its current exact contexts
have no remaining conditions. A separate expanded run covers all twenty-five
new theory roots and also has no residual. Both results match independent
reconstruction. These results establish readiness of the accepted source
versions, not truth of an earlier rejected draft or native mathematical-proof
admission. The revised byte-snapshot wrapper also successfully follows the
original method proposal again, preserving the same result.
