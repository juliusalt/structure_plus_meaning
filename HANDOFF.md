# Handoff: the planner's state

What a planner needs to act **now**, and nothing else. It is held by every knowledge base and read whole by every
designer, so it stays small on purpose. What was done and how is in `PLANNING_LOG.md`, which no base holds and
nothing plans from; a decision of the development itself is an entry of `DECISIONS.md` and here a reference; what a
task must respect is in that task's brief.

Reset on 2026-09-20: everything this file held was a record of what had been done, and it is kept whole in
`PLANNING_LOG.md`. The first planner of the next run writes this from the graph, the log, the working tree and the
history, as its charge asks.

## Graph

(the shape of the graph and why it has it — to be written when the graph has been taken in hand)

## Decisions

(what is decided and still bears on what comes, each with where it is written — to be written)

## Delivered

(what each finished task delivered, at the level later work needs — to be written)

## Open

(the open questions, the owner's with their provisional choices — to be written)

## Now

The run begins on a knowledge base built fresh and a harness that has changed. Nothing is carried over into this
file: take stock first — what has been produced and is not yet recorded, what the graph no longer needs, and what its
structure should be — and write this file as you go. `PLANNING_LOG.md` holds everything that stood here before.
