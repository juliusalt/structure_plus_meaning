# Handoff: the planner's state

What a planner needs to act **now**, and nothing else. Held by every knowledge base, read whole by every designer.
What was done and how is in `PLANNING_LOG.md`, which no base holds and nothing plans from; a decision of the
development itself is an entry of `DECISIONS.md` and here a reference; what a task must respect is in its brief. The
owner's words are in the ledger (`.claude/orchestration/owner-ledger.md`) and are cited here, not restated.

## Graph

**The direction.** The Q7 order is the owner's (Q7, answered 2026-09-24): the loop's notions as native definitions —
readiness, the verdict of a kind and request construction are native — then problems whose subjects are native
definitions, then the translation of native content into Isabelle. Its third step begins with the native loop's first
problem, the owner's (18:53): what a problem is, its native notion the loop's product and not a design's; then Q2's
problem (answered 17:18), how to choose the next problem to solve, posed over the first answer and exercising it, with
the authority question a native problem whose answer the owner approves. Every step answers the owner's rule of 18:12,
"Every distinction a native program relies on must come from a native notion, not from a HOL presentation relation",
which withdrew #373's notion of a problem (task 9's row at a locus, its subject a key). The entries the route rests
on, all accepted:

- #378, "The native loop's first problem is what a problem is; the problem of Q2, second, exercises its answer": the
  least form — an asked relation (a native program's entry read at a given and a candidate) and a given — posed from
  the owner record of 18:53; the answer native content, judged by the guard, admitted as a generation, verified by
  Isabelle before publication and approved by the owner (Q23).
- #376, "Every distinction a native program relies on comes from a native notion: the state the native loop judges and
  the presentations that retire": the given is the development's package read by the Factor grammar's native readers,
  with no rows, keys or families for it; request construction at a package a new notion; the bootstrap loop's
  presentations kept exact and unextended until it retires (Q24).
- #381, "The first problem's requirements use the test of a native distinction; the octet audit is one of its parts"
  (the owner's Q23 (a), 19:57): the guard's four sockets — G1 retention, G2 formation and closure, G3 the callee
  boundary, G4 the octet audit over the added definitions; the contract parts (a), (b), (c), (f) checked by Isabelle in
  the verification request; the rest criticism and the owner's approval (Q25).
- #383, "Non-nominality of uses is equivariance under use permutations, a contract the verification request checks":
  part (d) in the verification request, a native sample at a use-renamed copy in criticism, no fifth socket, uses
  staying structure (Q26).

**The first problem's route** (#380's, #382's and #409's proposals, each build with its review):

- first: the owner records (#385), the payload audit's native reader (#387), the members a package adds with the
  callee boundary G3 (#389), the equivariance notion (#420, `Presentation_Equivariance`, with the marking of #381's
  entry) and its use instance (#422, `Factor_Use_Renaming`, its imports below the reader theories #416 and #418 edit);
- then: the guard (#391, after #387, #389), the given's readers as one numbered program (#412, after #387, #389), the
  readers' use-equivariance clauses (#416 the package and scope readers; #418 the definition, clause, pattern and data
  readers and the audit, after #387; both after #422), the given installed beside the development package (#414, after
  #412 and N2b), the native request at a package (#410, after #422 and #416; the first request, its installation, is
  briefed later with the criticism build, Open 121);
- then: the guard installed over the given's readers, the asked relation's entry with its equivariance clause (#393,
  after #391, #414, #416, #418, #422), the given and the native state's first generation (#395, after #385, #414), the
  posing (#397), the answer judgment and admission (#399), its transport (#401), the verification's native half (#403),
  frame (#405, after #422: parts (a)–(d) and (f)) and harness (#407).

N2a (#340) and N2b (#342) join and compile the development package (#320's entry, as #376 corrects it), the given the
first answers extend; the given's installation waits on N2b.

**Beside the route**: the bootstrap loop's maintenance — the rows line's presenters proved in their domains (#354), the
answer harness's shared runs (#369), the listed-family law at the collection programs (#379) and its re-citations in
the development theories (#375) — and #384, #365's follow-ups over #356's seed round, which has landed (B4a #365, B4b
#367). Retired on #376's and #378's entries (their tables): the decomposition line (#325–#334), N1a, N1b, N3a,
N3b, N4a and N4b as briefed, #371, #377, the designation by locus (#169, #170), the index form's retirement (#92, #93)
and the machinery's verification stage judged natively (#193, #194).

**Landed lines, as the bootstrap loop keeps them** (their commits and figures are in `PLANNING_LOG.md`; none is
extended, #376; their remains are under Open): the verdict of a kind as native programs over a state's rows
(`Development_Native_Verdict`, `Development_Verdict_Witnesses`; #34–#46, #101, #288); request construction over the
state's rows (`Development_Native_Request`; #106–#114, #276, #277, #285–#287); the incremental assessment
(`Development_Incremental_Verdict`; #185–#192, #318, #353); the rows line, a row at a locus (#22–#30, #83–#97,
#161–#167); the decomposition's schema and library (#58–#64); the digit layout (#147–#159); the rule programs' law
(`Native_Collection_Programs`; #173–#275, #295, #374); the shared-subterm engine (#290, B1–B6 #297–#307); the
exporter's obligations at every state it defines (#227–#308, #352).

**Word changes are serialized** (Decisions): none is queued. A word change's records are re-recorded after its landing
by a task of their own (Open 100).

**Shape.** No build waits on a review task. The deepest chains are 9, the first problem's route through the given:
#387 → #412 → #414 → #393 → #397 → #399 → #401 → #407 → #408, the same from the native package (#340 → #342 → #414) and
from the notion (#420 → #422 → #416 or #418 → #393).

**Order** (the queue holds it; the planner's choice, a residual until the native answer to the selection problem
exists, Q2): the owner's directions first — the first problem's route, #420 and #422 at its head, then #412, #342,
#391, #414, #416, #418, #410, #405 and the rest in their chains' order; the tasks in their checks (#340, #379, #354),
so that they land; then #384, #375 and #369; each review after its build.

## Decisions

Taken by the planning episodes, where they are not entries of `DECISIONS.md`.

**The owner's directions, as the work carries them** (their words in the ledger):
- **Q7** (answered 2026-09-24 16:34): the order is the owner's; the loop's HOL-constant problems are the bootstrap
  loop, kept standing with the word changes and records that keep its checks green, and retired once the first
  problem's answer is admitted and problems are posed again under it (#378's entry, Q23 (f); Open 111); work that would
  only extend it is not planned.
- **Q2** (answered 17:18) **and 18:53**: the first problem is what a problem is, the second Q2's, posed over the first
  answer (Open 119); the authority question a native problem whose native answer the owner approves, deciding the
  approval of derivations and derived subproblems. Meanwhile readiness and the planner's choice are the working basis,
  each interim choice carrying a residual — the queue's order among them.
- **17:50, 18:12, 18:36**: the native notion of a problem is the loop's product, not a design's; #373's design (landed
  `93cae3bb`) was accepted and then withdrawn at the owner's question, its identity resting on HOL-assigned prefixes
  and keys. **A design is judged by what its identity rests on, not by what its contract avoids.** Every distinction a
  native program relies on comes from a native notion; a presentation is one member of its notion's class and supplies
  no meaning; a row, a locus path, a prefix, a key or a family index is structure, and a code a HOL function assigns
  supplies meaning only through HOL.
- **Nothing of the native loop is built over the bootstrap loop's presentations** (`development_problem`, task 9's rows
  and loci, their prefixes and keys, `readiness_presents`, `state_presents`, `development_rows_present`,
  `request_presents`), and no build extends one; a build that adds a reliance on a HOL presentation records it (#376,
  #378). Consolidations of existing native programs that extend no relation go on.
- **2026-09-23**: `native_control_plan.md` is the current plan with a current scope and absorbs none of `plan.md`,
  `OBLIGATIONS.md` and `GENERALIZATION_REVIEW.md`, which stay (carried out by #280, #281, #291).

**Word changes are serialized, and their order is the planner's** (plan-48): an edge orders two tasks whose landings
re-record the same report or record files; tasks whose files are disjoint run side by side. **A word change's answer
records are re-recorded after its landing, by a task of their own** (plan-51): a native record replayed in a tree whose
theories change the state's closure is unproduced by construction (Open 100).

**The engine's numbers.** A reach and, since the shared-subterm line, a judgment over a whole state are affordable: B6
(#307) measured the machinery judgment at 3.76–4.22 s and the seeded at 0.28–0.31 s (against #278's 121.1 s and 4.92
s); the machinery's bare reach 4.61 s, the seed's 0.14 s. A measurement states the quantity it counts; a stage and a
judgment are compared by demanded calls and by seconds, the ratio to a bare reach beside them (plan-51). Attribution is
by difference, not by the ML profiler (every profiling entry task 7 obtained had `count=1`). Not taken, with their
triggers: `finite_interface_fits`, an index instance, at a measured need; a positioned labelled history for the
certificate path (about 160 s a cycle), which needs a round-by-round correspondence the library lacks (Open 56).

**How tasks carry work.**
- A task told to continue after a landing (`v2.py after`) keeps the tree it had (q51) and brings main in before it
  hands over; a task that consumes another's landed theories otherwise waits on it, so its tree holds them.
- A mail is read by the session that receives it: a task continuing in a new session gets what its first session read
  by mail only through its brief, its tree or a mail sent again. What a task must respect goes into its brief.
- A task that must wait on a build a brief will propose waits on that brief as a stand-in, re-pointed (`v2.py
  blockers`) or spliced when the proposal is placed (#382, #409).

**Notions and their uses** (still bearing on work):
- A use of an index cites `Carrier_Indexes` and its carrier's instance and proves only its key's obligations; an
  argument made twice across uses is stated once in `Carrier_Indexes`.
- A notion whose contract names something downstream moves as its general form (q72): the locale and its general
  contract go where its kin are, the instance stays where its terms live, its statement unchanged.
- A composition the law states once is not kept as a statement of its own (plan-48): `read_clause` states it.
- A use cites the most specific contract that states what it needs: an instance's where one states it.
- A partial presentation is honest; a total one that maps the omitted case somewhere is not (Open 13).
- `Native_Execution_Refinements` can collect only refinements of constants declared before it; word equality cannot
  certify where a refinement attaches, so a refinement's acceptance carries liveness evidence (the export theories of
  the recipes it refines reach it, and their seconds moved).
- Transport stays a task of its own, not a design: a packet is the presentation of a request row, and transport a
  reader contract, the reading the inverse of the presentation (Open 8).
- A decision that cites rather than carries waits for a measured need in a question that is then expensive.
- Bootstrap loop only, kept and not extended: a question's candidate is keyed by the key of what it is (q21); the
  development rows' definitions take the state's constant-key assignment as a parameter with an `inj_on` premise.

**Working rules every brief carries** (as the harness stands since 2026-09-24; what it now does itself is not asked of
a task):

- **Hand-over.** In a task's own tree the hand-over is its files, the repository's check (`v2.py check`) and its
  `commit.md`, so an Acceptance may say "the repository's check" without its command. Batches and trains check the work
  merged onto main, so a task brings main in (`v2.py bring-main`) only when told its lines meet main's. The standing
  source checks and the import graph are answered at every change that writes a theory; the commit states the
  harness's record of the session's probes beside its check, so a `commit.md` Validation paragraph states only what no
  record does, never a check's outcome.
- **Naming.** Every theory a task works on is named in its brief (`theories/X.thy` or `X`) and every fact it consumes
  as `Theory.name`: its session is given their statements and relations as it starts, and their size comes out of its
  room. A Deliverable names files, never a directory; a bare name is the task's own folder's unless the root has that
  file, a new root file is `./NAME`; a brief names its folder `.build/tasks/<id>/`, never an edit's key.
- **Timings.** A timing is held (`v2.py measuring --exclusive`: every other run stops, bounded at 3 minutes,
  `.claude/orchestration/state/measure-bound`) or shared (`--shared`, told the machine's load); a brief that asks for
  one says which and sizes a held run to fit. A run re-measuring a quantity recorded elsewhere states the scale factor
  it measured and its absolutes in both forms. A before/after pair timed in the same holds is its own reference: its
  scale factor is a control timed with it whose code no commit between the two bases changes; a factor from an
  unrelated computation is not a scale; an estimate is compared first in the quantity it counts (q85). A numeric
  expectation written before the work chose its cases is an estimate, not a target.
- **Probes.** A change that writes a theory says `--probe` to be probed, and its reply carries the finding; a probe is
  an inner loop, not a gate (q26), bounded at 60 s unless a measurement holds the machine; its evidence is
  `probe.summary.json`. An edit to a base theory is probed from the tree, the unchanged theories between it and its
  changed dependents loaded as renamed copies. A probe from a tree behind main whose base advanced is refused, naming
  what differs only because main advanced: `v2.py bring-main`, or `--base` at the heap the tree matches, or
  `--from-heap` for one theory; a changed theory's `export_code … checking` is blanked in the probe's copy and named
  skipped; a past base's own sources are read with `--base-sources`.
- **Receipts and records.** The harness advances the base and retains the receipts at every landing; no task advances
  the base or retains. A brief whose work re-records a recipe's reports or answer records names each file in its
  Deliverable, and the landing check compares against those files (Q12). Answer records are replayed on a base holding
  their state closures as HEAD has them (#143).
- **Merges.** A task whose commit fails at the merge is queued again as it is; `bring-main` merges main, never a row by
  hand. Where both sides changed one THEORY_MAP.md row, `bring-main` is refused, so the round hands over main's row
  verbatim (its line as main holds it, set by `=== replace` at once) and leaves its change in `rows.md` for a later task
  made from main; where the work changed that theory's imports, the round moves what needs the new import into a theory
  above it and sets the theory and its row back to the branch point's text (#165, q76; Q13). The row additions follow
  in a fix made from main after the landing, or in the first task made from main after it; a task that changes one of
  those rows waits on it. A task in the one tree installs its theories only at hand-over and keeps nothing of its own
  uncommitted there when it parks or comes back (q30).
- **Text with backticks goes through a file.** `v2.py reply`, `tell` and `ledger` given their text as an argument pass
  it through a shell, which strips a backticked name: write such a text to a file under the planner's drafts and pass
  `--file`.
- **Size.** A brief's Size is judged against its room with its own relations — the theories it names can be the greater
  part of it — and a task beyond its room is handed back at its first start. Such a brief is trimmed to the piece of
  reasoning that belongs together, its corollaries and its moves becoming tasks over what it leaves, never by dropping
  what the kept work must respect.
- **Scope.** Small repairs of the same theories, and follow-ups consolidating what is stated twice, go into one task up
  to about half its room; a repair another task waits on, or one of a notion's contract, stays its own task. A task that
  continues one task's work says `continues` and forks that task's session while it is warm. A partial task whose
  deliverables are written is re-planned, not split. No task rests on reasoning not yet written. A fix the planner
  creates carries no review task. A design entry is not rewritten when the implementation narrows what it predicted:
  it gains a marked correction.

## Delivered

What finished tasks delivered that open work builds on; the rest is in `PLANNING_LOG.md`.

- **The entries the route rests on**: #378 (`cbf0c7e9`), #376 (`47a67cfe`), #381 (`2d97747c`), #383 (`7b23938c`), each
  with marked corrections of the entries it changes (#373's, #320's "The problem", "Its rows" and "The builds, in
  order", task 9's, #378's, #376's; #381's part (d) is marked by #420); #378 recorded the owner's words of 17:50, 18:12,
  18:36 and 18:53 in the plan, with stage 2 and its gate; their owner choices are Q23–Q26.
- **The route's briefs**: #380 (#385–#408), #382 (#410–#419; #393 and #395 on #414), #409 (#420–#423; #393, #405,
  #410, #416 and #418 on #422).
- **#320** (`b30c3d33`, design): DECISIONS.md "Problems about native definitions are posed, answered and judged at the
  development package's rows" — the development package joining the six notions' programs (N2a #340, N2b #342), its
  rows and row-level request, verdict and edit corrected by #376's and #378's entries; its owner choices Q18.
- **#374** (`1db8e167`): the rule family's law over a listed family of triples (`native_listed_law`), the applied rule
  returned as its triple, `step_rule` without a set difference; DECISIONS.md "A listed family returns the rule it
  applied". Its review's follow-ups 1–5 at the law's own theories are #379, 2 and 6 in the development theories #375.
- **#288** (`93cae3bb`): what the verdict's fields and their witnesses both prove stated once; its review's follow-ups
  1–2 to #375, 3–5 Open 118. **#353** (`09cea44c`): the per-answer function's last shared and edit-sized reads; its
  review's follow-ups Open 120.
- **#356** (design): DECISIONS.md "The published state holds its targets once, and a report's word is read off them";
  B1 #359 (`d212cb53`, `Shared_Term_Words`), B2 #361 (`35d48aba`, `Represented_Snapshot_Transactions`), B3 #363
  (`254b5999`, `Presented_Publication_Values`), B4a #365 (`1a8677a4`, `Development_Seed_Referenced_Publication`; its
  review's follow-ups 1–3 taken by #367, 4–8 #384), B4b #367 (`1f60a87f`: the seed recipe reads the publication's word
  off the word constant, 33.14 s saved in its held pair, `.build/tasks/367/measurement.md`; its review's follow-ups
  under Open 27 and the next-edit list); #355 (`2c69e833`), the cause's formation discharged where the judgment
  constructs it.
- **The answer harness**: #313 (`e9bbe02b`) the framed answers' parts read in one session, #323 (`ec46e355`) the judged
  answers in one shared proof session, #314 (`d47ce451`) the records re-recorded on #167's base, #319 (`9d84c95f`) the
  re-record in the harnesses' one form; their follow-ups are #369 (with #319's per-answer seconds) and Open 122.
- **#167** (`04c20f87`): every report switched to its problems' rows in context and the problem datum's tags retired;
  its review's follow-ups 1–5 are #354.

## Open

The owner's questions, a line each (their words and the provisional choices in full are in the ledger):

- **Q1** the admission rule of a bootstrap adoption and OD-2 — provisional: the predecessor library fixes the rule and
  an answer supplies only accepted entities; bites when an admitted native answer is installed as Isabelle material.
- **Q2** answered 17:18, and at 18:53 made the second problem (Decisions).
- **Q3** what Isabelle establishes about adequacy — provisional: truth and formal adequacy only; unplanned.
- **Q4** an agent executor confined to its packet — provisional: none run; the plan's stage 3 gate.
- **Q5** how far the native residual record reaches — provisional: by demand from the loop's notions.
- **Q7** answered 16:34 (Decisions). **Q8–Q12** answered by the harness (parked tasks in queue order; the `.pyc`
  removed; the base and the receipts at every landing; a brief's named report files in the landing snapshot).
- **Q13** the harness merging a THEORY_MAP.md row both sides changed — provisional: the #165 pattern (Working rules,
  Merges).
- **Q14, Q16** when an applicable split decomposition is demanded — provisional: the split source empty; they bear on
  nothing planned since the decomposition line retired. **Q15** is void (Q16 says so).
- **Q18** #320's five choices — publication after Isabelle's verification (a), the payload condition (b), a notion's
  HOL change re-deriving the package as a residual (c), machinery residuals superseded by native problems (d), the
  first native problems a residual record (e, superseded by the answer to Q2 and 18:53) — provisional; its text opens
  with "Q17", a slip of plan-53's.
- **Q21** withdrawn with #373's notion (Q22 says so).
- **Q23** #378's six choices — (a) answered 19:57 (#376's test, its native form #381's); (b)–(f) provisional: the least
  form, the owner's approval as a record, verification as a request after admission, the first problem's decomposition
  as a generated method, the end of HOL-constant problems.
- **Q24** #376's four choices — the test for a native distinction, the native loop's state read by the Factor readers,
  the bootstrap loop exact and unextended, an executable counterpart as a native reader's implementation — provisional.
- **Q25** #381's split of the test — four native sockets, the contract parts in the verification request, the rest
  criticism and the owner's approval — provisional.
- **Q26** #383's three choices — non-nominality of a use as equivariance under use permutations; checked by Isabelle,
  a native sample in criticism, no fifth socket; uses staying structure, a use leaf in the term language the owner's to
  choose — provisional.

**To plan, in the order expected** (numbers kept from earlier states):

3. The translation of admitted native content into Isabelle material — the verification problem's request, answer and
   admission, where Q1 and Q18 bite: the design after the first problem's builds; N7 (the verdict at a package) when a
   problem kind asks it, N8 (installation) after the first answer.
119. The second and the authority problems' requirement families, posed over the first problem's admitted answer
   (stated before it, the test would shape what it tests), with #370's follow-ups (`.build/tasks/370/result.md`:
   interim choices recorded natively, a choosing problem's contract kind, the loop the two problems enter) and #371's
   notes (`.build/tasks/371/notes.md`).
121. The first problem's criticism build, briefed with the first request (#410's installation) once the route's
   proposals are placed: native samples of invariance (two presentations of one subject) and of use equivariance
   (#383's follow-up 4: one permutation moving every use of the candidate's environment outside it, one permuting its
   uses among themselves), observations of ground clauses and target leaves, each failure recorded against the
   candidate (`Observation_Repairs`); with #381's follow-up 3, the leaf argument for programs with material premises
   (`Factor_Positive_Parametricity`), at the first answer whose added definitions hold one.
111. The HOL-constant loop's retirement (#357's follow-ups, `.build/tasks/357/result.md`): when the first answer is
   admitted and problems are posed again under it, the machinery's HOL residuals superseded (Q18 (d)), the seed's
   refinement problems no longer selected or issued, the records kept as history; #320's "Beside the bootstrap loop"
   gains a marked correction.
116. `edited_reach_system` stated as the rooted restriction (`Factor_System_Restriction.rooted_system`) of the package's
   joined program at O's admission roots (#321's review, follow-up 2): a task once N2a (#340) lands.
4. A proof request class and a proof problem's decomposition; its first question, the native content of a proof
   answer, is where Q1 bites.
5. The refusal an absent certified generation cannot tell from an unavailable input: an empty result and a failed one
   kept apart.
7. A persistent native published state: without one an adoption records the transaction of its judgment rather than one
   against a history (item 81 waits on it).
8. Transport, the octet direction's task 6: a reader contract, the reading the inverse of the presentation.
9. The plan's stages 4 and 5: the harness, the adoption tool and the checks have no notion in the state (what moves the
   active base; one run limit for every tool spawning Isabelle; the commit's evidence paragraph).
39. Native production of a request body — forward evaluation of a functional native relation, an engine notion the
   library lacks; until then producers are executors and the construction admits.
40. Currentness of a request after an admitted answer: not designed.
49. Origins and authorities no construction assigns yet (item 30 with it): the observation naming one is owed at the
   first construction that assigns one — the owner records (#385) are the first.
99. Installing an n-clause program into an existing native source grows ×2.4–2.5 per doubling (#309's figures, its inner
   stages not separated; `.build/tasks/309/runner`), and environment formation superlinearly (#159): attributed when an
   installation on the route (#393, #414) or a native question over hundreds of candidates needs it.
100. A native record replayed in a task tree whose theories change the state's closure is unproduced
   (`export_proved_code` refuses): replays run after landing; a proof context holding the tree's theories would lift it
   — a tools task at a measured need.
27. The seed recipe's cost (#324's attribution, then #367's held pair, `.build/tasks/367/measurement.md`): the word's
   lookup is read off the word constant, 33.14 s saved against the pair's own before, which drifted from #324's
   absolutes (lookup 34.90 against 28.15 s, recipe 50.82 against 48.50 s: characterized before any claim against
   #324's figures). What remains: the payload judgments (8.03 s, most of it certifying a cause that presents its
   payload twice; that entry's Open), the word's table (3.87 s) and the check's load — in #367's landing checks the
   publication presentation took 45–48 s unheld against 17.68 s held, and the seed recipe still set the recipe phase
   (47.7 and 45.27 s; the machinery's 43.0 and 42.86 s). The presentation under load is attributed before the
   machinery recipe is, at a measured need of the check's cycle; it can gain at most the 4–5 s by which the seed leads
   the machinery recipe (#368's follow-ups 1, 2, 5). #355's formation saving by phase and the fall from #128's 63.45 s
   are unattributed.
78. The probe tool (#316's, #286's and #353's reviews): the comment's "lower envelope"; skipped code checks extended at
   first use; the prelude route offered by the tool; a refusal naming `--from-heap`; a probe that loads
   `Native_Collection_Programs`' dependents passes 60 s — a longer bound or an intermediate heap at a measured need, the
   check certifying them meanwhile.
29. `REASONING_REUSE.md`'s open comparisons (#280's follow-ups): the ordered comparison factored at its next use; the
   six grammars' retention theorems against one statement over `RRA_Read_Environment` (`problems.txt` condition 3); the
   known-predecessor refinements against `Established_Premises`; ordinary clause profiles against `native_rule_law`.

**At a theory's next edit or a measured need** (one line each; the details are in the review each names, and the full
text of each as it stood is in `.build/plans/plan-61/HANDOFF-before-condensation.md`): 12 the selection locus's tag (a
word change of the seed's publication; bootstrap). 13 a locus for a multi-constant problem (bootstrap). 17 the replay's
timeout stopping its whole tree (`run_harness`, the adoption tool). 18 `Isabelle_State_Difference`'s embedding facts
once. 20 the rows' answer family (retired with the rows line's growth). 22 the probe's last command from Isabelle's
position report. 23 `development_definition_sockets` through the singleton reading. 24 the index notion's query domain
per carrier. 25 the index entry's two slips. 28 recipes' execution boundary without `tools/build.py`. 31
`readiness_presents_rekey` into `Development_Native_Selection`. 35 #80's entry's figures. 36 the equality contract
derived once. 41 a constructed request's constant stated. 42 `ffilter_empty_set` and `ffilter_true` into
`Candidate_Generators`. 43 the engine-level forms #85 left. 44 `demand_positions_member`'s form and
`Development_Located_Rows`' dead imports. 45 the host-test record through a result class. 46 a recovery theorem for
installed finite programs. 47 the demanded-closure facts into `Finite_Demanded_Closures` (one argument: a closure
carried by an injective map). 48 formation checks that grow (`finite_object_formed`). 50 a request's context through
kind tags (bootstrap). 51 the `some` readings through the subject index. 52 term quotation depth. 53
`finite_fresh_addresses` in one pass. 54 task 124's entry naming design 136's. 55 the overnight boundary, historical
at `aa946a22`. 56 the certificate path's keyed cost on the verdict. 57 facts downstream of their notions (reach
membership, `appended_new_names`, `distinct_ordered_remdups`, `entity_row_with`). 59 the judgment bridge's candidate
position. 60 stale overnight map rows. 61 `development_stated_native` beside `declarations_single_valued`. 63 the
answer's formation check. 64 the subject index's build in one pass. 65 `known-history`'s two failures. 66 a presented
state's families computed once. 67 `import isabelle_places` in two tools. 68 the record candidates' arity. 70 the
receipts' form. 71 the establishing facts into `RRA_Finite_Environments`. 72 entity keys across rounds. 73 #218's
follow-ups (the adoption criterion, restated arities). 74 #185's naming (`.build/tasks/196/review.md`). 76 cosmetic
(#241's, #284's and #312's reviews). 77 `entity_row_kept`. 80 #191's first review (`.build/tasks/202/review.md`). 81
an adopted answer's precondition at its receipt's revision. 82 inherited-ignored signals. 83 the replay's cost split
per step, with #369. 84 the presenter's entity keys through an index, near 10^4 entities. 86 order facts named for
their user. 87 one definition per reading of a name table. 89 the adopted walk answer's restated
`indexed_heads_exact`. 90 the verdict's three unbuilt witnesses and its entry's two sentences (#44's review;
bootstrap, at a consumer). 91 `Development_Publication`'s unread renaming lemmas. 92 the exporter's remaining
obligations (`.build/tasks/352/review.md`). 93 the storage report theories' word identity, and the documents check
refusing a tracked ignored path. 94 the documents' passes (#291's review). 96 #294's review (the sort's code, the name
index, the per-answer tree). 98 `keyed_reference_insert` into `Keyed_Value_References`. 101 a found key's two
comparisons. 102 set-composition facts into `Finite_Set_Composition`. 103 #284's in-place correction marked. 104
#311's review (the shared view's names). 105 no identity presenter for the rows (bootstrap). 106 the two payload audits
in one form. 107 the whole judgment of a refused edit (bootstrap). 108 V's admission (bootstrap). 109 #309's review
(the listed union's row, one fold per shape). 110 the settled rounds and the rule table's union in a judgment
(`.build/tasks/307/result.md`). 112 `read_index_address` stopping at its terminator. 113
`Development_State_Presenter`'s row wording. 114 the rows' cost in the HOL-constant loop's reports (not planned). 115
a shared-term table read through an array. 116b #363's review (the worklist comparison, facts moved, one `term_view`
renamed). 117 #361's review (`finite_singleton_option_image`, the identity map's `_on` form). 118 #288's review
(key-injectivity re-cited, `answer_statements_entities` moved). 120 #353's review (the incremental assessment's
remaining scans; bootstrap). 122 #319's review (the framed records' one-line form at their next re-record; the two
demanded answers' failure unattributed). 124 the word constant's equation exported as a checked contract,
read by the runner, once a second report takes a word constant (#368's follow-up 4).

## Now

- **Mailed, read by each task's first session**: #313's re-review's first follow-up and #319's per-answer seconds to
  #369.
- **Classifier stops**: a classifier stopped plan-51 to plan-54, plan-56 and plan-58 to plan-61 before their notes, and
  #323's fixer once; each next planner took up what HANDOFF.md held, and nothing was missing. Repeated declines are the
  run's to handle, not ledger questions (the owner, through the monitoring session, 20:23 and 20:28).
- **The harness's, met by this run's tasks**: `v2.py read check:167` raises a TypeError in `check_text` (#168's review,
  follow-up 7); `measuring --shared` wrote no `measurements.log` (#307's runs, #306's review); bases swept before a
  brief's task ran (#324; #315 exported them again from its proof context); the finalizer's push of `82a065cd` failed;
  the finalizer should commit the handed-over files only (#303's tree held untracked `.bashrc` and others); the
  exclusive hold outlasts its run (#317); a tree behind main cannot probe until it brings main in (#316); a re-review
  writes over its first verdict (#65's, #302's); `v2.py ledger` records a text beginning with `--` (Q15). Owed to the
  owner: each landing check's phases and per-recipe comparison; earlier gaps are in `PLANNING_LOG.md` (2026-09-24).
- **Ledger entries that can leave** (the harness's `v2.py ledger`): Q7's and Q2's questions and answers, recorded in the
  plan (`4434635d`, `82a065cd`); the owner's words of 17:50, 18:12, 18:36 and 18:53, recorded by #378 (`cbf0c7e9`).
- **Trees without a running task**: those of the retired #169 (thirty changed files and one commit, not to land),
  #325, #371 and #324; `.build/trees/143` holds task 143's uncommitted six-line note in
  `validation/development-answers/README.md`; `.build/trees/176` is superseded (#243 landed its work) and still stands
  with uncommitted changes, which makes the open-tree overlap checks inexact until it is removed — the harness's or the
  owner's.
- **The base lives at the lasting place** `.build/tasks/base-lasting/`, advanced by the harness at every landing;
  `.build/complete-20260921a` and `/tmp/structural-accepted` are the owner's or the next reboot's. The one tree's
  `.claude/orchestration/` changes and the sandbox's untracked placeholders belong to no task.
- **The standing refusals, so they are not re-derived**: the retirement of `Development_Truth`, kept and presented
  partially; an observation naming problems with a non-singleton subject; re-opening #16's key-assignment parameter;
  the attribution run and the interface index as tasks; teaching the check to refuse an unresolved import before it
  builds.
