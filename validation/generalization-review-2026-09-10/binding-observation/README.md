This retained module constructs native binding-observation premises and feeds
them to the existing learned-rule generator and inference engine. The complete
53-case family and 21 source computations match independent reconstruction.
The earlier 52-case run and exact checker bytes remain in their own directory.

The dependency proof covers the exact 327-theory source context of the export
and the positioned-specialization and report-totality roots. Whole-project
validation also checks the fixed native program theorem.

The source computations return both complete lists and the exact established
frontier. The learned schema joins that frontier at the same context and input.
A successful row conversion is not pattern or whole-argument admission; the
control family includes a paired value outside the pattern-observation image.

From a checkout matching the source digests in proof.json, replay with:

    python validation/generalization-review-2026-09-10/binding-observation/replay.py --poly /path/to/poly --output /tmp/binding-observation-replay

The proof receipt retains original execution paths. The module, checker,
inputs, and complete outputs here preserve the evidence for this checkout.
