The retained module executes the complete 38-case reasoning family. Eight
additional cases exercise the ordinary specialization-report rule at 342;
both native reader premises remain unproved in those structural controls.
The thirty earlier cases and six input starts are repeated regression checks.

The proof receipt covers the module's exact 300-theory import context. The
separate whole-project build covers the other symbolic-scheme theorems. The
receipt's paths identify the original execution; the files here retain its
module, complete output, inputs, and checker.

From a checkout with the source digests in `proof.json`, replay with:

```sh
python validation/generalization-review-2026-09-10/specialization-report/replay.py --poly /path/to/poly --output /tmp/specialization-report-replay
```

The checker verifies complete schemas, bindings, premise occurrences, generated
applications, residuals, demand, and reasons against an independent valuation
enumeration. Native semantic guarantees require the accepted theory contracts
and evidence for every known premise. These controls do not supply native
mathematical-proof or whole-symbolic-argument admission.
