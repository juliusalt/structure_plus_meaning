# Complete binding clauses and guided investigation

The problem is to recover actual native binding judgments from their complete
program and requested goals without known traversal seeds. The full
seven-clause library comes from the actual program. The original generator
leaves fourteen distinct native calls unresolved despite entry coverage.

A general theorem identifies variables unavailable from ordinary premises.
Every actual binding clause has complete variable scope in its head. Requested
heads can therefore supply complete valuations, subject to the original
instance and material checks. Guided rounds add premise requests and possible
conclusions while preserving the caller's original known evidence. Universal
closure and monotonicity proofs retain all earlier bounded constructions.

The 46-case guided family checks 188 complete intermediate states, 22 native
source computations, twenty complete native libraries and wrapper calls, and
every final report field. Fifteen cases gain settlements. At the checked deeper
bound, the length-zero through length-three true native goals settle with an
empty known set. False goals stay unresolved. The original 73-case family,
50 compiler cases, 65 coverage cases, and 51 reader cases also pass. These
families overlap and should not be counted as independent discoveries.

The assumed stage deliberately adds requested goals to known evidence. Its
twenty conditional computations match reconstruction, but the policy falsely
settles native goals on two formed workloads. Its policy assessment fails.
Successful conditional reports do not establish the supplied evidence.

The two method investigations apply the existing generic basis and revision
machinery to all twenty native workloads and three methods. Starting with
soundness alone leaves three residual distinctions and returns 148 repair
witnesses. Feeding back its exact selection {0,1} removes every residual.
The assessment preserves truth and the original input gate as mandatory
requirements. The returned profiles identify guidance as a strict improvement
among the two eligible methods. Case selection and method proposal remain
external steps; the finite assessment does not establish wider coverage.

## Retained evidence

The index contains 25 proof contexts, nine reasoning stages, both method
investigations, and attachments. The complete 802-theory session passed in
invocation d89e2882-f6da-42f8-9032-4d8544752f35 with unchanged sources and tools.
The final execution export comes from the accepted 355-theory context.
Failed proof attempts, the Java startup failure, and command timings are kept.
Some early original sources were reconstructed from their retained effective
bodies and verified against every recorded digest; the retention does not
misrepresent these as contemporaneous source copies.

Every manifest entry is verified by digest. Identical bytes reuse references
to the preceding reader-construction archive within this repository. Complete
cached proof sources, original tools, generated code, cases, runtime programs,
raw logs, and independent method reviews are retained. The Poly/ML executable
is the external runtime and must match its recorded digest. Original logs may
name temporary paths; the index maps their exact bytes into this archive.

From the repository root, use a fresh output directory:

~~~sh
python3 tools/replay_reasoning_review.py \
  --archive validation/generalization-review-2026-09-11/binding-guidance \
  --stage guided \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly \
  --output /tmp/new-binding-guidance-replay
~~~

The before and assumed stages replay their complete original case families.
Replace --stage guided with --investigation binding-method-initial or
--investigation binding-method-followed to replay a complete method result.
The replayer checks every complete machine report and independently
reconstructs it. It does not repeat proof checking or candidate selection.
Additional replay and tool-check artifacts are indexed by replays.json.

The six reader source conditions remain open. The binding computations do
not establish native source readings for the reader fixture's environment
markers. Whole argument admission, mathematical-proof admission, and coverage
of every development decision also remain open.

All four final replays pass: 46 guided cases, twenty conditional assumption
cases, and both complete method investigations. The assumption policy still
fails native semantic adequacy as recorded in the original policy assessment.
The 27 tooling tests also pass; their raw output is retained with the replays.
