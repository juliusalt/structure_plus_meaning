# A complete executed finite native reader

The finite example supplies actual package, replacement-record, target-schema and inference-node sources. Its complete data input retains the original two artifact uses, old binding and two counted attachment occurrences inside a three-use extension with a separate node and a second binding. All twelve public argument fields are independently recovered.

The accepted 403-theory source batch proves the local inference-specialization judgment and each of its eight initial call occurrences in the same native program. The two empty reference instances coincide, giving seven distinct initial calls. The final entry 350 is absent from that initial evidence. The full 817-theory repository check also passes, in invocation `fdf1222a-254d-4441-9410-02755f0d91aa`.

Thirty presenter cases recover all source fields and distinguish attachment modes, multiplicity, use identities and malformed sources. All 76 reader cases pass: 51 retained structural controls and 25 complete-source cases. They check 311 complete construction frontiers and 75 native-library executions. The original complete source goals settle at depth 3 and remain settled at depths 4 and 5. Withholding any distinct required initial call leaves entry 350 unresolved. Altered source, report and support fields remain unresolved; a malformed unused input fails the whole input gate.

The source adapter checks every known subset against the actual proved exported reference set. Original source goal references and changed computational controls have explicit separate roles. The earlier binding-only truth check remains unchanged. The rejected host-reference attempt and all preparatory proof failures are retained as diagnostics.

The index retains original proof receipts, exact generated code, complete cases, runtime programs, every result, source snapshots, decision inputs and actual followed revisions. Large files are stored losslessly: `archive.json` gives both stored and decoded identities. The replayer verifies the stored files, materializes only the selected stage dependencies, and checks each decoded digest before using the original checker. No recorded source or execution file is rewritten.

## Reproduce the retained executions

From the repository root, with the recorded Poly/ML binary available:

```sh
python3 tools/replay_reasoning_review.py --archive validation/generalization-review-2026-09-11/finite-native-reader --stage presenter-current --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly --output /tmp/finite-presenter-replay
python3 tools/replay_reasoning_review.py --archive validation/generalization-review-2026-09-11/finite-native-reader --stage native-input-21 --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly --output /tmp/finite-input-replay
python3 tools/replay_reasoning_review.py --archive validation/generalization-review-2026-09-11/finite-native-reader --stage native-reader --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly --output /tmp/finite-reader-replay
```

Every retained development investigation is also replayable with `--investigation` and its index key. Replay repeats the original code, complete outputs and independent assessments; it does not substitute for a new Isabelle proof check. `python3 tools/check.py --threads 12 --timeout 900` checks the current complete source session.

This example closes the finite executed local-reader requirement. Whole-derivation admission, native mathematical-proof admission and the continuing repository-wide audit remain separate work.
