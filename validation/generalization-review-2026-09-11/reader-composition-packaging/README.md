# Verbatim evidence and the whitespace check

The complete whitespace scan of commit 1d3ac86 reports seven trailing-blank-line warnings, all in verbatim archive objects. The code and documentation pass their separate whitespace check. All seven retained hashes match the expected original byte identities; normalizing those endings changes every identity.

The complete comparison keeps evidence identity, code/documentation whitespace, and archive whitespace as separate observations. The exact returned revision is executed. Requiring all original evidence identities and the passing software check selects preservation of the verbatim bytes. The archive-only style exceptions remain explicit; the complete whitespace scan is not described as passing.

The retained case includes the actual scan output, each original and normalized digest, the unchanged accepted 807-theory validation, and all original byte objects. The normalized alternatives are measured in memory. No evidence bytes are rewritten. Both retained comparison reports replay successfully.
