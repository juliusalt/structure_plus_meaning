# Complete conditional reader construction

The preceding library-coverage report required conditional construction of
the native heads at 348, 349, and 350 from their actual clause premises.
The case family takes every schema field from the exported native libraries.
A reusable host fixture expander constructs only test inputs. The native
investigation receives the original component calls and requested goals.

Both the initial and combined-library families pass 51 complete cases,
including 195 intermediate frontiers, 50 selected libraries, 50 coverage
reports, and one native binding-source computation. The final family calls
the combined native wrapper in 43 cases; seven cases select fewer
constructors. Every returned application and report is independently checked.
The original 53-case, 50-case compilation, and 65-case coverage regressions
also pass. These overlapping families are not separate discoveries.

All six constructors are proved to be actual clauses of the same formed
inference-specialization program. Its selected-library closure theorem is
conditional on the original known calls and holds at every finite depth.
The existing native binding computations establish their own calls there.

The original frontier has eight calls. Two native binding observations are
established; the complete report, specialization, two record observations,
reference bindings, and node reading remain unproved. Environment markers
are structural controls, not evidence of native source readings or an
inhabited reader instance. All 19 public fields, four shared-field joins,
private row bindings, distinct sockets, and unproved conditions remain
explicit. Whole argument and mathematical-proof admission and complete
development-workflow coverage remain open.

## Evidence

The complete 795-theory session and 27 Python tooling tests pass. The full
session receipts identify invocation fc5cc21a-7abb-4739-94c9-413380611cde and
retain unchanged source and validation-tool digests.

The index records each proof, run, original input, and attachment.
The archive manifest verifies every retained object and every reference to
the preceding reader-libraries archive. Those references reuse identical
bytes within this repository. The Poly/ML binary remains an external runtime
whose exact digest is checked.

Two proof attempts timed out; the third passes the complete 347-theory
context. An attempted raw export of the polymorphic list library failed.
The typed baseline and combined aggregates export successfully. The failed
attempt remains reproducible as a failure.

A first replay accepted all 51 complete reports but failed its additional
raw-log digest comparison because compiler warnings name the relocated
module path. The incident summary is labeled as a reconstructed account;
both raw logs and the original replayer are retained. The corrected replay
checks every complete machine-report line and preserves raw diagnostics.
Replay runs reuse the retained case inputs; they do not repeat case
selection or establish new native source evidence.

From the repository root:

~~~sh
python3 tools/replay_reasoning_review.py \
  --archive validation/generalization-review-2026-09-11/reader-construction \
  --stage after \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly \
  --output /tmp/new-reader-construction-replay
~~~

Use a fresh output directory. The before stage replays the original
five-constructor family. The catalog-polymorphic-failure stage reproduces the
retained type failure. The source projects and original checkers are
materialized from exact retained bytes, independently of the current checkout.

Both full 51-case families replay successfully. Their complete reports are
checked against independent reconstruction. The final replay and the guarded
catalog replay are retained under replays. The current retention and replay
tools also reject execution with Python assertions disabled; tool-controls
retains those checks and the follow-up tool versions. These host validation
checks do not establish additional native source readings.
