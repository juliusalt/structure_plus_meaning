# Actual native source composition and prospective development decisions

The independently stated problem is to construct an actual native inference-specialization source while preserving every original source field. The previous source review ruled out all payload-marker source requests. The initial conditional investigation instead demands three reusable bridges: complete specialization binding observations, fresh proof-node installation, and preservation of the binding relation under package-source inclusion. An explicit finite executed example is a fourth, separate requirement.

All three bridge contracts and their complete composition now pass Isabelle. The universal native reading keeps the original package fields, replacement and target presentations, support lists, and exact behavior of every old artifact and binding. The node has an empty discharge relation, as permitted by this local reader. This proves no whole-derivation validity for a rule requiring child proofs.

The original and factored dependency methods run the same nine complete workloads. With native source-reading contracts withheld, the factored method still settles the universal local-construction goal in two additional controls. Both methods preserve the mandatory whole-input and matching-proof conditions. Following the comparison machinery's exact returned revision leaves no residual distinction, and its adequate profiles support the factored method. Requiring the finite executed example leaves no eligible candidate.

The proof attempts are retained, including proof failures, a timeout, and a launcher failure before proof checking. The final native context is readernativecomposition8. Serializing the retry avoids overlapping Isabelle startup; that successful retry does not by itself prove the cause of the earlier launcher failure.

The development-decision family separately submits work and scheduling, information needs, proof repair, statement adequacy, first-use reuse, validation, and workflow coverage. Subsequent type and adapter decisions and the startup scheduling decision use the same process before their dependent actions. Earlier manually chosen repairs remain earlier workflow failures; these runs do not correct them retrospectively.

The reusable decision adapter takes independently supplied complete cases and mandatory observations, executes their reports, follows the exact returned revisions, and reuses the existing assessment. It reproduces all fourteen complete reports and all seven assessments of the original family. Its disabled-assertion, duplicate-name and path-escape controls reject before creating an output directory. The separate conditional-case adapter retains the declared cases and extracts every returned residual and unconstructed demand; these demands are not generally a sufficient repair set.

Candidate proposals, English interpretations, and the link between named mathematical contracts and conditional clauses remain external. The finite decision inventories do not establish all-problem or all-method coverage. An executed finite native reading and mathematical-proof admission remain open.

`index.json` records the proof contexts, full construction runs, decision investigations, original sources, generated code, and exact input bytes. `archive.json` verifies every retained reference. Replays and full-session validation are recorded separately in `replays.json` when complete.

The package environment is extended to install the node; its presentation
changes accordingly. The original package coordinates, replacement and
target presentations, and exact old artifact and binding behavior are retained.

The full 807-theory session passes in invocation
ae847506-9e9a-4c01-836b-efe77d7eeeac with unchanged proof sources and validation
tools. All thirty-nine retained reports replay successfully. The complete
replay outputs, full-session receipts, and final documentation are retained
in replays.json.
