# Necessary source requirements for the original reader cases

The problem is whether the original conditional reader requests can be turned
into actual native source readings. Guidance preserves all 51 original cases,
including their input gates, applications, settlements, and six remaining
conditions. All 245 intermediate states are independently checked.

A complete environment presentation requires separate artifact and binding
tables in a pair. The actual node reader supplies such a source witness, and
the inference-specialization reader contains that same node reading. The
necessary condition is proved for all remaining fields. Failure of its
executable shape check rules out every native environment witness. Shape
success is not environment or reader admission.

The source family retains the original problems and adds four controls.
All 55 complete reports, 265 guided states, 54 native libraries and coverage
reports, and 56 source occurrences in 54 requirement reports are checked.
Every one of the 52 original top-reader occurrences has a payload environment
and is impossible as a native judgment. Three paired controls pass the shape
check, but their reader goals remain unresolved. A malformed pair fails the
whole input gate. A target source fails the necessary shape. The previous
46-case binding-guidance family also passes against this export.

## Assessment through the existing machinery

The source comparison includes all 54 cases with source requirements.
Observing only structural input formation leaves 151 missing distinctions and
151 repair witnesses. The existing revision operation adds the source-shape
observation. Feeding back its exact selection removes every residual and
returns an unchanged revision. Both complete results pass independent review.

The two paired controls meet the declared necessary screening conditions.
Neither supplies a fully admitted native reader source. The assessment therefore
requires an actual constructed source and proofs of its complete reader
premises. More clauses or deeper search cannot complete the payload-marker
requests. This finite screening comparison does not establish broader method
coverage, whole argument admission, or native mathematical-proof admission.

## Evidence and replay

The index retains the original checked export, the accepted 272-theory source
requirement context, and the accepted 357-theory execution context, together
with all three complete reasoning runs and both successful comparison runs.
The failed first revision attempt is retained as an attachment and has no
evaluator result: a case-builder file changed after its digest was recorded.
The reusable reviser now points prior evidence at the preceding run's exact
archived bytes while preserving the original paths as provenance. Its
successful retry changes no logical case field or returned selection.

Every manifest reference is verified by digest. Identical objects reuse the
preceding binding-guidance archive within this repository. Original source
and tool versions, cached proof snapshots, generated code, complete inputs,
raw logs, assessments, and the rejected invocation remain available. The
external Poly/ML binary must match the recorded digest.

From the repository root, use a fresh output directory:

~~~sh
python3 tools/replay_reasoning_review.py \
  --archive validation/generalization-review-2026-09-11/reader-source-requirements \
  --stage after \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly \
  --output /tmp/new-reader-source-replay
~~~

The before stage replays the original guided 51-case family. Replace
--stage after with --investigation source-initial or --investigation
source-followed to reproduce a complete comparison result. Replay checks
all machine reports and independently reconstructs them. It does not repeat
proof checking, case selection, or native source construction.

Final main-session, tooling, and replay evidence is indexed by replays.json.

The full 804-theory session passes in invocation
c886634d-5833-4fee-866c-7e95762943b6 with unchanged sources and validation
tools. The 27 tooling tests pass. All three final replays pass: the full
55-case source family and both complete source comparisons. The final shared
adapters also reproduce the previous method study and verify its retained
evidence. Their actual scripts and raw output are retained in replays.json.
