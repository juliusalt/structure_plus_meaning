# Structural observation replay and delivery evidence

The [original archive](../structural-observations/README.md) contains the
structural comparison, binding construction proofs, complete executions, and
their exact input sources. This supplement retains every completed replay,
the initial replay-assessment failures, their diagnosis, and the corrected
replays. Its summary accounts for all twelve execution stages and the one
dependency investigation. Both expected rejection stages reproduce their
original outcomes.

Three application-comparison replays initially reproduce every generated
report exactly but reject the assessment's Python tuple representation of
mandatory pairs. The receipt stores those pairs as JSON arrays. A bounded
comparison is derived from the actual archived checker results and complete
stored assessment objects, with recursive type information retained. The
identity operation fails on all three nonempty families; the existing JSON
copy matches the whole stored object on those families and the empty family.
The comparison and its exact revision select the JSON copy under the mandatory
whole-object equality requirement.

These observations come from the actual host operations and complete values;
the generic basis evaluator consumes the resulting table conditionally. This
does not establish an Isabelle contract for arbitrary Python or JSON code.
The subject objects, operation source, typed output, original inputs, and
equality equation are retained separately from their finite-table assessment.

The replayer now applies the existing whole-object JSON conversion before
comparing the reconstructed assessment with the original JSON value. It drops
no field and preserves ordered occurrences and repeated values. All five
accepted application-comparison and binding-contribution stages affected by
that shared branch pass their corrected replay. The other completed replay
paths retain their original results. Exact source and code reproduction is
separate from rerunning kernel proof checking.

The delivery source check revalidates every theory, `ROOT`, and build tool
against the accepted 826-theory invocation. No source is missing or unlisted,
and there are no proof escapes. All 50 tooling tests pass; the subsequent
replayer correction is exercised by the five complete affected executions.
The staged whitespace scan for the implementation, documents, and original
archive is empty.

Replay the two bounded assessment investigations with the common replayer,
using this archive and `--investigation initial` or `--investigation followed`.
The recorded interpreter identity is checked. Their complete reports and
original independent assessments remain reproducible. Native mathematical
proofs, whole-derivation admission, the older observation subject boundaries,
and the final repository audit remain open.
