# Structural observations and compatible binding contributions

This archive retains the complete structural problem, its first five failing
construction examples, the proposed contracts, the executed method comparison
before integration, and the subsequent proofs and executions. The original
problem and integration decision are attachments in `index.json`.

The comparison observes complete schema problems through actual construction
operations. Functional maps connect every index to its candidate, condition,
and complete problem object. `application_observation_subject` identifies each
observation with the corresponding condition on those objects. The three
conditions require complete valid instances, preservation of the earlier
application relation, and inclusion of the supplied required applications.
The last condition is independently specified before comparison.

On the four original problems, the initial two conditions miss a distinction.
The returned residual is `(1,0)`, with repair witnesses `(1,0,2,0)` and
`(1,0,2,1)`. Following that revision adds condition 2 and leaves no residual.
Only the joined operation meets every mandatory condition. It returns four
applications for the three-variable problem and one for the partially observed
premise family; the former operation returns none. Both retain the two complete
baseline applications. This finite comparison does not prove universal
preservation or justify the meanings of older observation adapters.

The integrated operation recovers bindings from actual schema projection
matches, combines whole compatible fragments, then applies the original full
scope, schema-instance, premise, and material checks. Every generated binding
is recovered by its complete supporting observation family. Mandatory groups
retain every observation; pools can offer alternative groups. Requests and
possible premises supply binding information without establishing premise
truth. Source roles retain ordinary sockets and callees and all five material
operands. Universal theorems preserve both earlier application sources and the
original bounded guided soundness contract.

The existing pattern-instance operation replaces its proved equivalent
duplicate. Requested construction and propagation share the original complete
premise enumerator. General matching and finite-set composition contracts are
factored at their first common use.

`index.json` contains 35 proof receipts: 17 proposal attempts, 12 integration
attempts, and six derived code receipts including the original baseline code.
Failed proofs and the interface failure are retained. Integration 29 accepts
414 source contexts, checking 54 against 360 complete unchanged parent
contexts. The complete 826-theory session passes in invocation
`981593bc-0c6d-4bd1-8529-c3fae0b86d1d`; its exact records are attachments.

The twelve execution stages include the original five problems; the rejected
interface and both accepted comparisons; 22 direct observation groups; 16 new
guided cases with 64 complete states; the original 55 guided cases with 265
states; repeated, empty, and ambiguous subject families; fresh proved native
source preparation; and the original 76 native reader cases with 311 complete
frontiers. All accepted stages pass independent reconstruction. Ambiguous
subject indices are rejected even when the underlying conditional table is
formed. Repetition and reordering preserve the comparison; an empty family
provides no preference between the operations.

Every mathematical report in the 76-case native reader family agrees with its
earlier execution. In the guided family, 215 reports or intermediate states
gain conditional applications or their possible calls and requirements. Every
original residual goal and input-formation result is unchanged. The complete
differences and a lossless shared-subobject presentation are retained; these
finite comparisons are separate from the universal preservation proofs.

One source-readiness investigation is also retained. It concerns actual proof
dependencies and does not select the construction operation. The complete
application comparison supplies that bounded development evidence. Candidate
proposal, independent criticism, coverage of the wider workflow, whole native
derivation admission, and native mathematical-proof admission remain separate.

Replay a named execution stage from the repository root:

```sh
python3 -B tools/replay_reasoning_review.py \
  --archive validation/generalization-review-2026-09-11/structural-observations \
  --stage comparison-current --poly /path/to/the/recorded/poly \
  --output /tmp/structural-observations-replay
```

Use `--investigation integration-source-readiness` for the dependency report.
Each replay verifies retained identities, restores exact original tools and
code, reproduces every complete output, and reapplies the original assessment.
The two rejection stages must reproduce their original failure. Replay does
not rerun Isabelle proof checking or establish unproved subject meanings.
Large blobs use lossless storage with separate stored and decoded identities.
