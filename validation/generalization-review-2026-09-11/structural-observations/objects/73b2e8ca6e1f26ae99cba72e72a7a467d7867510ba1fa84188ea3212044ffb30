{
  "candidate_status": "Unregistered external proposals; only completed proof and subject-bound execution can support integration.",
  "actual_observation_requirement": "Each table index must be linked by a proved equation to the complete candidate, condition and workload objects used by the computation. No prose facet rating is supplied.",
  "implementation_candidates": [
    "Separate_Application_Sources",
    "Joined_Application_Observations"
  ],
  "comparison_conditions": [
    "Every returned tuple satisfies the original complete schema instance and material predicates.",
    "Every original complete application is retained.",
    "Every required concrete application tuple is present."
  ],
  "input_relation": "The workload contains the complete schema, actual premise enumeration, frontier, requests and required application relation.",
  "interpretation_limits": "Candidate constructors identify actual formal functions. Conditions refer to actual returned applications. Inclusion of a requested application does not prove its premises true; known facts remain separate.",
  "additional_reviews": [
    "One mandatory group retains every observed input; a search pool may offer alternatives.",
    "Different schema projections and variable bindings must not be joined across unrelated schema applications.",
    "Conflicting observations must never produce a single-valued relation claiming both different values.",
    "Duplicate enum operations must have a universal equivalence proof before deletion.",
    "Old arbitrary observation tables remain conditional data and do not establish the intended subject of a facet."
  ]
}
