# Explicit execution in the development workflow

The owner's instruction requires using the generalization machinery for every
problem of interest, including decisions about development and the machinery.
The previous file said that the machinery must participate. The agent also
failed to apply the stronger instruction consistently. The wording study
does not excuse that behavior.

Three exact texts are retained: the original, an explicit execution and
feedback proposal, and a minimal execution-only amendment. The original
natural-language reading is externally supplied. Its first combined facet
incorrectly credited the minimal amendment with both execution and result use.
That input error is retained; it was corrected by separating the requirements
before selecting or applying a text.

The corrected investigation starts from scope-only observations and returns
three residual distinctions. Its exact returned selection is executed again,
removing all three. The generic mandatory-condition assessment requires all
six independently stated conditions, including conditions unnecessary for
distinguishing these candidates. Only the explicit proposal satisfies them.

The chosen text is applied after the corrected run, revision, rerun, and
assessment. The application record preserves both original and applied
identities. The complete cases, reports, code, tools, proof contexts, evidence,
and external interpretation are retained by index.json and archive.json.

This verifies a wording decision under an explicit external interpretation.
It does not establish that the agent consistently follows the workflow,
automatically interpret English, admit native mathematical evidence, or
retroactively make earlier development decisions machinery-driven.
