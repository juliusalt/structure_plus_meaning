# Constructive candidate search: delivery evidence

All 118 conditional investigations and all ten final native stages in the
[construction archive](../candidate-construction/README.md) replay successfully.
The [replay index](replays.json) identifies every complete accepted receipt by
its original stage and exact bytes. Native mathematical fields and original
independent assessments are preserved; only the declared clocks vary.

The complete session accepts 836 theories. All 77 tool tests pass, including
historical parent lookup, complete procedure-input retention and corrupted
retained-copy rejection. The whitespace-only checker correction preserves its
parsed program and every generated native program on all 30 supplied cases.
The governing plan and continuing review state the constructed language,
whole-fragment boundary, formation gate, inclusion minimality and remaining
native-admission obligations.

The first replay batch exhausts storage while preparing the complete reader
case family. Its input JSON is 1,067,592,473 bytes. The first 126 replay records
are accepted; the reader and finite-presentation stages lack accepted records.
The partial 326,017,024-byte reader input and all available failure evidence
remain retained. This interruption is not a mathematical rejection.

Command access becomes intermittent under the quota. A read-only recovery run
compares all bytes of four temporary copies against the decoded retained input,
including the end of each file. It then executes both stages of the conditional
machinery through standard input, verifies their checked code and interpreter,
and independently reconstructs their full results. The existing coded table
is reused with exactly the same finite arguments; the new storage subjects and
host-operation claims are recorded separately. After the complete results and
reasons are read, a final unchanged-byte check precedes removal of precisely
those four copies. This frees 4,270,369,892 bytes. No workflow exception is used.

The two unfinished stages then pass in fresh output directories. The original
126 accepted stages are not repeated. The recovery argument is subsequently
reexecuted in the standard receipt format, preserving both complete native
results exactly. That reexecution supplies portable replay evidence; it does
not retrospectively select the earlier action or assert that deleted paths
still exist.

Restored source, helper and decoded-input copies are identified in the
[copy map](replay-copy-map.json). Every occurrence maps to its exact retained
bytes; data already retained in the construction archive is referenced rather
than independently reinterpreted. Complete replay outputs and the interrupted
preparation remain distinct attachments.

The standard supplemental investigations are `storage-initial`,
`storage-followed`, `documentation-initial` and `documentation-followed`. Use
`tools/replay_reasoning_review.py` with this archive, `--investigation` and the
selected name, the recorded Poly/ML executable and a fresh output directory.
Their conditional table results are separate from the host byte comparisons
and from native semantic proof.

Candidate construction is complete for the declared source-site language under
its explicit gate. Whole native symbolic-derivation admission, native
mathematical-proof admission, full development-workflow coverage and the final
repository audit remain open.
