

## Constructive candidate languages and scoped search

The owner's next review identifies the candidate axis as incomplete: the two
finished application operations have no declared search language, and the
comparison constructs neither from parts. The same review requires a practical
scoped method alongside the complete compatible-union meaning.

`Finite_Compatible_Unions` now owns that complete meaning independently of the
application client. `Finite_Directed_Contributions` searches for every supported
binding whose domain is exactly the requested variable scope. It branches on a
variable with few available values, keeps compatible whole fragments and checks
complete support. Its result is proved equal to the exact-domain restriction
of the full compatible-union relation. No fragment may lose a conflicting or
out-of-scope field through projection. The original full-union operation remains
available, and scoped search does not truncate the supported answer family.

`Finite_Candidate_Assembly` declares every sufficient subset of a supplied part
basis. It chooses an uncovered requirement with few supporting entries, adds a
supporting part and removes redundant parts. Construction succeeds exactly when
that language is nonempty. Each returned plan is inclusion minimal, with a
requirement lost upon removal of each retained part. This does not claim minimum
cardinality. Input order and repeated presentations may change the selected
plan while preserving the finite language and existence result.

The application client supplies actual observation sites as parts. Requirements
carry complete problem, target and variable subjects. Coverage is calculated
from whole accepted matching fragments contained in the target binding. The
shared observation-assembly theorem connects that coverage to the complete
application relation, including every original instance and material condition.
Mandatory applications contain both the former operation's results and the
explicitly required targets across the complete problem family.

The formation gate retains schemas, all observed terms, the complete original
premise enumeration and the exact target presentation. Invalid inputs remain
distinct from valid inputs with uncovered requirements. Empty variable domains
still pass the complete gate. The language concerns subsets of the supplied
site basis; it does not enumerate arbitrary programs or development edits.

Exact requirement maps and decoded-witness equations permit indexed execution
without replacing the original subjects. Coverage caching preserves the
constructor function. The native reports retain complete inputs, mappings,
coverage, application results and removal witnesses. Small bases execute every
subset as an independent check of the declared language.

The executed scope includes 61 generic cases, 30 complete application cases,
8,256 overlapping binding fragments over 128 variables and a joint application
basis of 240 sites with 224 requirements. All 55 original guided cases and 76
native reader cases preserve every complete mathematical report, including
conditions and reasons. Malformed inputs, missing and conflicting fragments,
repeated presentations and zero-variable targets have explicit controls.

The complete 836-theory session passes in invocation
`6f81afd8-4c5e-4b1c-ab08-500ff03e0264`. All 77 tool tests pass. The
[retained review](validation/generalization-review-2026-09-12/candidate-construction/README.md)
contains the source proofs, successful and failed attempts, complete native
executions and prospective conditional investigations. Its input audit binds
all recorded proof, export, parent, helper and runtime inputs to recoverable
bytes. The [delivery evidence](validation/generalization-review-2026-09-12/candidate-construction-delivery/README.md)
records complete replay outcomes and the interrupted storage attempt.

The retention review recovers two historical drivers only under their original
recorded identities. A shared execution helper now retains every nonexternal
checked input before execution and checks complete retained coverage, live
hashes and retained hashes. Proof entries retain their procedure inputs, and
parent metadata is read from its exact retained bytes, including compressed
earlier evidence. These operations establish byte provenance, separately from
the mathematical observation equations.

Host-code and documentation proposals in this review remain externally
constructed and conditionally assessed. The native construction demonstrated
here has the stated source-site language. Whole native symbolic-derivation
admission, native mathematical-proof admission, complete development-workflow
coverage and the final repository audit remain open.
