{
  "problem": "The retainer preserves exported code but omits recorded procedure-input maps from its proof entries. It also parses the live parent receipt after retaining a potentially different historical identity. Bind proof procedure inputs explicitly and parse the exact retained parent bytes.",
  "requirements": [
    "Retain every explicit export, source-proof, parent and helper input in each proof entry.",
    "Reject inconsistent identities for a shared recorded input path.",
    "Recover optional parent-proof links from the original retained parent receipt, including compressed prior evidence.",
    "Preserve the original proof sources, native reports and archive format compatibility.",
    "Validate with changed-live-parent, complete procedure-input and compressed historical-data controls, then retain and replay the entire real construction batch."
  ],
  "files": [
    "/tmp/generalization-quality-review/candidate-delivery-review/proof-retention-draft1/tools/retain_reasoning_review.py",
    "/tmp/generalization-quality-review/candidate-delivery-review/proof-retention-draft1/tools/test_retain_reasoning_review.py"
  ],
  "evidence": [
    "/tmp/generalization-quality-review/candidate-delivery-review/complete-input-audit2.json",
    "/tmp/generalization-quality-review/candidate-delivery-review/export-input-recovery.json",
    "/home/julius/structure_and_semantics/tools/retain_reasoning_review.py"
  ],
  "criticism": "The existing construction archive can retain exact bytes through generic attachments, but its proof entries do not bind these procedure inputs. Reading a changed live receipt is outside the identity established by the archive lookup. The correction makes those identities explicit without treating them as semantic justification."
}
