{
  "problem": "Construct application candidates from declared structural parts and find sufficient compatible binding contributions without enumerating irrelevant unions.",
  "owner_requirements": [
    "Candidate construction must operate on actual parts and state its search scope.",
    "The completeness claim must identify the candidate language it covers; ranking a supplied list alone is insufficient.",
    "Separate the meaning of every compatible contribution union from an executable search for the required scope.",
    "Use the generalization machinery prospectively on concrete subjects, inspect complete results and reasons, and independently criticize the scope and evidence.",
    "Validate on basis sizes large enough for the intended applications."
  ],
  "proposed_candidate_scope": "All subsets of an explicitly supplied finite set of actual schema-observation source sites. A plan filters observations by those sites and joins their complete recovered bindings. This is a scoped constructor family, not the space of all possible algorithms.",
  "binding_scope": "All functional unions of complete input fragments whose domain is exactly the demanded variable scope. The existing unrestricted compatible-union relation remains the semantic reference.",
  "selection_requirements": [
    "A missing-variable choice and each compatible extension must preserve existence of every complete target binding.",
    "Each generated application plan must use only declared source parts and meet every original required application, including preservation of the original complete-source results.",
    "If any plan in the declared finite subset language meets the requirements, construction must return a plan; failure needs an uncovered requirement.",
    "No returned plan part may be removed while preserving every original mandatory application requirement. This is a finite workload criterion, not global algorithm minimality."
  ],
  "proposal_boundary": "The first bounded experiment uses explicit complete host objects and independently reconstructed outputs. Its finite observation table is conditional until the proposed Isabelle contracts are checked. No host execution is presented as native proof checking.",
  "inputs": [
    "original-comparison-problems.json",
    "original-contribution-controls.json",
    "original-guided-contribution-controls.json"
  ]
}
