{
  "user_request": "Reread the workflow document, adhere to its scheduling requirements, and require a full read after every context compaction.",
  "observed_problem": "The active work used many short proof-repair cycles, repeated source reads and separate status polls. Checking the same root after each local correction did not make those a sufficiently prepared content batch.",
  "proposal": "Add a repository AGENTS.md entry point requiring a full workflow reread immediately after every compaction; add the same resumption requirement to the workflow; update the active checkpoint; review all remaining proposed application proof bodies, API changes and runtime-adapter needs before the next code batch.",
  "remaining": "Persistent instructions do not demonstrate compliance. Subsequent scheduling must group the independent work and inspect combined results. No semantic or native proof claim is made by this documentation change.",
  "next_batch_information_needs": [
    "Complete application proof diagnostics and every remaining proposed proof body, especially repeated rewriting and subset arguments.",
    "Current application export signature and reusable source/term/result encoders required by the native application driver.",
    "All prepared original, large, impossible and malformed application controls and the expected report distinctions."
  ]
}
