# Final candidate delivery verification

[Verification](verification.json) retains the complete four supplemental replay
receipts, the 128 primary replay identities, both delivery manifests, and the
current accepted 836-theory source identity check. The raw supplemental native
outputs and programs are retained as attachments in [the index](index.json).

The prospective cleanup investigation retains both complete finite results and
all reasons. Its [decision](decision.json) distinguishes conditional host
claims from native calculation. The [copy map](runtime-copies.json) records
all 3,012 replay files removed after complete identity checks; each maps to
its exact retained bytes in the earlier permanent archives. The [cleanup
receipt](cleanup.json) records the bounded action.

Source proofs, executable contracts, host validation, archived byte identity,
and publication checks are separate evidence. The wider native-admission and
repository-audit obligations remain open.
