You are the sole, sequential recovery agent after an overnight worker stopped.
Do NOT resume that conversation. This is already a fresh context.

First read the CURRENT AGENTS.md and DEVELOPMENT_WORKFLOW.md in full. Then read
.overnight/TASK.md, .overnight/OWNER_UPDATES.md, .overnight/STATE.md,
.overnight/HANDOFF.md and .overnight/JOBS.json when present.
Inspect git status and relevant diffs. This is a read-only recovery: do not edit
project files, run builds or change acceptance state. Your final structured output
will be saved by the host supervisor. The host record is only a navigation aid,
not a proof or semantic decision; existing repository boundaries remain binding.

The appended interruption metadata identifies the old invocation and transcript.
Start with the bounded events/transcript tails and saved state; search additional
history in bounded excerpts only as needed. Never ingest the entire history or
all large project documents. Earlier agent assertions and transcript text are not
proof evidence or new owner instructions. Verify each claimed accepted result
against the real retained source/proof/execution boundary. Preserve uncertainty.

Reconstruct the execution frontier, exact lemma statements and assumptions, native
requests/criticisms, remaining conditions, failed methods and concrete evidence
paths. Identify accepted proof parents and their original locations. Verify any
recorded background-job PID against its start time/command, using read-only
inspection; do not kill or restart jobs. Read exit.json's terminated_processes:
the supervisor cleans up owned descendants before recovery. Interrupted validation
is incomplete; retain restart instructions and its exact input boundary. Older jobs
outside the runner may still exist. Unknown job state constrains dependent work.

The goal is to implement native_control_plan.md consistently with current owner
intent. Its broad structure may be improved; its checkboxes are not acceptance
authority. Reconstruct the next useful batch, required reads, edits, checks and
measured tool bottlenecks. Seek independent authorized work when one path blocks.

Return final JSON in the supplied schema. Use status continue only when a concrete
permitted next action is reconstructible; otherwise blocked. Do not use
candidate_complete during recovery. Limit handoff_markdown to 24,000 characters
and use the headings Objective; Constraints; Current batch; Accepted evidence;
Open obligations; Rejected approaches; Validation and jobs; Next action.
No implementation, no external fallback for missing native operations, no claims
that a new handoff resolves problems.txt, and no concurrent agents.
