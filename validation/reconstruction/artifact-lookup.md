# Complete indexed artifact lookup

The original artifact-membership relation determines every reader condition.
A binary relation store represents all complete input rows, including duplicate
rows, conflicting values and malformed artifacts. Use paths distinguish absent
uses from present empty words and retain arbitrary natural coordinates.

Seven actual readers run on ten complete subjects. The original scan and exact
indexed lookup are selected and adequate. Singleton filtering, a byte restriction,
refusal, confused optional uses and added formation checks each have a native
counterexample. All-input equations establish the two complete implementations.

```sh
python3 -B tools/reconstruct_artifact_lookup.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/artifact-lookup-reconstruction
```

All thirty-five reports reproduce complete inputs, original and candidate
readings, conditions, comparison and revision reasons, and every before/after
lookup in the counted updates. The path count is separate from preparation,
artifact equality, formation, actual generation-store adoption, complete
workflow enforcement and full physical cost.
