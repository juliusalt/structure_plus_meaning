# Admission of actual recorded causes

The original `certified_base_cause_at` relation determines admission from the
complete generation environment and site, generation core, requested whole
payload, recorded judgment, and separately retained proof environment and root.
The cause reader recovers arbitrary complete quotation layouts and every
permitted environment enumeration. Its finite checker has an all-input equation
to that original relation.

The native fixture constructs an original requirement decision, certificate,
closed replay and joined generation record. Every failed position remains
explicit. Coverage requires an actual successfully certified seed. The test
requirement policy and history are explicitly empty; their adequacy for the
full development workflow remains unresolved.

Twelve methods run on fifteen actual cases. Native selection and adequacy retain
only method 0, whose all-input theorem establishes the original condition.
Cases independently change proof material, roots, literal identity, leastness,
the generation source, scope inclusion, quotation layout and enumeration.
The use-word case containing 300 rejects an extra byte restriction. A changed
whole program artifact preserves package, application and closed replay
readings while failing recorded scope inclusion, exposing a method that omits
that condition. Shifts and reversed enumerations remain valid quotations.

```sh
python3 -B tools/reconstruct_certified_causes.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/certified-causes-reconstruction
```

The 36 reports retain every complete source, recovered scope and reading,
method decision, condition, comparison and revision reason. The serializer
streams large fields without omitting them. Reader specialization follows
proved complete quotation support and preserves every original result.
This boundary does not establish complete workflow enforcement, historical
permission, unrestricted criticism coverage, physical cost or genesis.
