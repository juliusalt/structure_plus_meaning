# Complete concurrent history reconstruction

The execution retains all 422 original native reports and one complete
artifact dictionary. Actual values are shared under proved complete equations;
every original subject, method, negative control, optional result, observation,
comparison, revision and source correspondence remains present.

Isabelle's existing `Parallel.map` implementation evaluates independent input
contexts, candidate inspections and initial-selection cycles. Its exact map
equation preserves order and repeated indices. Source-only scopes and actual
computed cause results are prepared once. Successful checked history results
replace omitted-guard computations only under their actual sufficient premise;
failed checked results retain those computations.

The ordinary packet entry point and its exposed stages have an unconditional
complete-result equation. Source projection and observation lookup require the
actual packet and cell construction. Artifact references preserve the complete
carrier, incidence, counted data, functional data and every occurrence. Timers
and digests supply no semantic verdict.

Run this recipe with the repository's declared Isabelle2025-2 and Poly/ML 5.9.2
bootstrap:

```sh
python -B tools/reconstruct_concurrent_history.py --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly --output /tmp/reconstruct-concurrent-history --threads 16
```

A direct `tools/check_concurrent_history.py` execution accepts
`--workers` and defaults to sixteen. The runtime uses Isabelle's Pure heap for
its established parallel-list implementation; standalone SML export does not
activate that implementation.

The original source boundaries remain unchanged. Full workflow construction,
all six conditions in problems.txt, historical permission and reachability,
native mathematical-proof admission, genesis and the final audit remain open.

The accepted shared cold reconstruction materializes 843 source files and rebuilds
788 theories from HOL, with no supplied project heap. The complete proof, every
original and archived execution input, all diagnostics and every full native
report were checked. All 422 original reports directly equal the measured
warm run; the additional stored record is the complete native artifact dictionary.
