# History construction using both established local premises

The complete closed operation composes the known predecessor constructor with
policy construction from the actual quoted scope. The original closed history
invariant and indexed membership establish every requested predecessor reading.
Successful original replay and quotation establish the complete scope and
certification used by the policy check.

The actual membership and literal replay guards remain. The operation still
computes quotation, package and policy alignment, target formation,
distinctness, anchors, allocation, predecessor bindings and literal grafts.
It preserves the entire original optional closed result: refusal, material,
head, fixed header, ordered ledger and exact cache.

The generic constructor congruence quantifies every payload and quotation
target at the fixed material, locus and rows. The existing guarded history
step composes that equation with its complete append. Existing injective
optional result maps establish equality of the entire closed result.

Twenty-three methods run on all sixteen original subjects. The composite
operation, both preceding refinements, original digit operation and bounded
reference are selected and adequate at indices 0, 1, 2, 4 and 5. Every complete
value from the three valid added producers equals the original typed result.
All nineteen original methods retain every complete result and native
observation at indices increased by four. Every original source and subsequent
input retains full native correspondence.

The additional raw control omits indexed membership while retaining all other
actual operations. It admits case 3, where readable generation material remains
outside the admission ledger, and fails both original result conditions.
Its complete result equals the original omitted-membership control on that
case. Readiness, replay and policy therefore do not replace membership. This
control returns raw candidate results and is never coerced into an admitted
closed history.

The original missing-policy, missing-replay, altered-state, changed-head,
stale-cache, unavailable-input and empty-family controls remain explicit.
The empty family stays sound and incomplete on all sixteen subjects. Every
successful subsequent state retains the original head, material and ledger.

Initial facet selections [], [0] and [0,1] are formed. Their residual counts
are 292, 22 and zero, with 2,696, 254 and zero repair witnesses. All revisions
use both facets and every followed basis is formed with no residual.

The complete native boundary contains 422 reports and retains the entire
2,252,583,546-byte output through lossless compression. Its source-only boundary
contains 710 theories, 31 Python files and the complete expected report boundary.

The fresh reconstruction rebuilds all 710 theories from HOL without a
reused parent context. All four stages succeed and every complete parsed
report matches the earlier 422-report execution directly. All
742 source-boundary files and 6 recipe inputs are verified,
along with all 738 earlier and 738 reconstructed
execution inputs and their archived originals. The complete retained archive
replaces 881,454,408 bytes of generated proof, database,
diagnostics and native execution copies.

The full prospective 1,442-theory integration check and all 102 host tests
with 59 subtests pass. All forty-one preceding complete source closures,
toolchains and original verification records remain unchanged.

The production operation removes two repeated reading paths. The complete
comparison still runs all original methods and emits every full result; its
end-to-end execution time remains substantial. The owner requires execution time to
become manageable before continuing problems.txt. Performance work proceeds
independently while this integration finishes. Full physical cost, all six workflow conditions,
historical permission and reachability, native mathematical-proof admission,
genesis and the final repository audit remain open.


## Native presented report (2026-09-17)

After accepted paired validation, this recipe now retains only its native word:
551,708 bytes, one complete report record. Every prior subject and
field remains in that presentation, with stores identified through their original
views and each distinct artifact given once. The word boundary is unchanged.
The current presentation took 246.90 s under the combined schedule.
See [the current measurements](replay-presentation-measurements.json) and
[current verification](current-verified.json). The earlier source-only counts and
JSON report counts above describe historical boundaries.
