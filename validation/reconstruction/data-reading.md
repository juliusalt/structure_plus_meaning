# Complete data reading from native source

The fixture is the complete cause returned by the existing native replay and
joined generation constructor. The entire keyed constructor family is retained;
an absent, failed or conflicting family supplies no source. No copied output
artifact is needed for reconstruction. The constructed source and all 31
original comparison reports were directly equal to the earlier complete input
and results; the fixture family adds one report.

Seven methods run on twelve actual cases. Formation, arbitrary root coordinates,
the complete interior, empty inputs, counted duplicates and failed readings
remain distinct. The original complete quotation relation independently defines
soundness and completeness. The selected original, prepared and root-filtered
methods also have all-input equivalence theorems. Preparation shares the source
formation check; complete quotation support restricts possible roots without
requiring a canonical coordinate.

```sh
python3 -B tools/reconstruct_data_reading.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/data-reading-reconstruction
```

The 32 complete reports include every actual source, reference and candidate
reading, computed condition, comparison and revision reason. Physical timings
remain diagnostics. This scoped specialization and source boundary do not
establish complete workflow enforcement, coverage, decision cost or genesis.
