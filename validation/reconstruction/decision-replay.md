# Original-requirement decision replay

The complete original source, requirement family and term family precede every
candidate. `finite_requirement_decision_replay` preserves the entire original
decision and constructs the native graph, application and least replay
environment for each admitted target certificate. Each result has its own
environment. A successful empty decision has an empty replay family; an
unavailable decision remains unavailable.

The operation instantiates the original source-constructor entry contract and
the universal native replay contract. A term failing any original requirement
is excluded. The certificate family has exactly the required complete keys and
is functional. Actual graph placement retains both repeated indexed edges and
different calls using the same leaf proof.

The investigation compares twelve actual operations on the twelve complete
original subjects. It computes eight original decision conditions, exact replay
family coverage and functionality, and all ten original replay conditions for
each returned certificate. The full 2,736 family condition positions and 1,890
individual replay positions retain their actual inputs, results and reasons.
Only method 0 is selected and adequate.

Omitting an original requirement can still produce a replayable new guard;
the original decision condition rejects that omission. Extra valid replays fail
the required-family condition. Missing rows differ from unavailable row values.
Erased mappings and call interiors have their own failures even where replay
succeeds. An enlarged retained environment fails least retention and closed
replay. All full comparisons and 2,849 initial and 320 partial repair witnesses
are retained by the reconstruction boundary.

Repeated native reads use caches constructed from complete actual inputs.
One shared exact-cache kernel establishes lookup and fallback equality; a
context-projection contract preserves every full public table field. All 29
reports agree before and after sharing, including indices, order and repeated
rows. Runs took about 812 and 678 seconds under concurrent work; these timings
do not establish the complete physical cost bound.

~~~sh
python3 -B tools/reconstruct_decision_replay.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/decision-replay-reconstruction
~~~

The source boundary contains 583 files: 555 theories, 27 Python modules and the
expected complete-report comparison. The shared reconstruction runner rebuilds
the contracts from HOL and executes the native operation. Host serialization,
hashes and reproduction do not supply any subject's meaning.

The least replay environment can omit an old package wrapper while the full
original source and decision remain separate outputs. Replay of the installed
guard does not establish original-policy or historical permission. Complete
workflow subjects and enforced transitions, candidate and criticism coverage,
history permission, the full cost account, native mathematical-proof admission
and genesis remain open. This composition does not close `problems.txt`.

A fresh source-only materialization rebuilt all 555 theory dependencies and
reproduced every field in all 29 reports. Complete source and diagnostic review
preceded removal of the generated copies. The [current verification](current-verified.json) records the complete
1,142-theory build, passing host tests and twenty source-only boundaries for
1,213 complete reports. Three changed recipes were freshly reconstructed;
seventeen unchanged complete input boundaries retain their earlier verification.
