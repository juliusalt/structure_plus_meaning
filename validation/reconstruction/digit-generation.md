# Persistent digit generation construction

General record construction now accepts any fresh record use and proved literal
embedding. Fresh-frame, literal-extension, selected-predecessor and whole-record
recovery contracts establish the requested generation and every original cited
site while preserving all old material. The existing constructor remains an
exact instance of that general account. No fresh embedding is declared
intrinsically canonical.

The bounded reference chooses Some [n] for the record and [n+1] for its literal
prefix, then advances the actual stored bound to n+2. The digit implementation
uses actual point readers and anchor selection, stored-head allocation, guarded
predecessor binding updates and a cached graft of all three literal targets.
Generic optional bind and sequence projection preserve every absent result.
Original readiness discharges every stage guard, so the whole optional result
equals the independently established bounded constructor on every closed input.

Seventeen actual methods run on twenty-four subjects. Complete operation
soundness and completeness are registered original-subject observations. Every
result also carries the original nine generation conditions, complete field
readings and actual predecessor references. Typed construction and the bounded
reference are selected and adequate. The old allocator passes all original
generation conditions on available inputs but differs from this particular
complete reservation operation. Its validity is retained explicitly.

Controls expose lost bindings or old material, changed predecessor values,
result uses, counters or payloads, refusal, a no-op, an empty result family and
conflated unavailable input. Omitting actual readiness admits cases 6, 7, 9 and
15. All four resulting environments are formed; their claimed generations are
invalid. They include duplicate generation values, an invalid locus anchor and
an incorrect predecessor claim. Formation alone cannot replace the complete
generation check or original readiness.

All sixteen original inputs remain complete sources. Further subjects execute
zero, one, two, four and sixteen actual constructions, using each returned
store, generation and site for the next request. After sixteen constructions,
the input retains 66 artifacts, 64 bindings and head 42. The next operation
returns 70 artifacts, 68 bindings, record use Some [42] and head 44. Three
boundary-only reservations retain two artifacts and one binding while advancing
the head from 10 to 13; the next record uses Some [13] and returns head 15.

Every complete typed input has a native equality check against the independently
executed original bounded input. Initial source bytes are compared directly
with the preceding original generation execution. Displayed finite-set
enumeration may differ; its order is preserved in the reports and does not
substitute for native equality of the original relations.

```sh
python3 -B tools/reconstruct_digit_generation.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/digit-generation-reconstruction
```

All 201 theories were rebuilt from HOL. Every one of the 78 complete reports
matches the preceding execution, and all source, database source, runtime input
and archived input bytes were verified. The three initial selections have 144,
16 and zero residual distinctions, with 2,192, 186 and zero repairs. The first
revision selects [1,0]; the other two select [0,1]. All retain both required
facets and leave no scoped residual. The actual revision order is preserved.

Record generation does not establish cause truth or historical permission.
Actual replay and history adoption, full physical cost, all six `problems.txt`
conditions, complete historical permission and reachability, native
mathematical-proof admission, genesis and the final audit remain open.

## Accepted native presentation (2026-09-17)

Both the original comparison and native word passed on the same actual export.
The word contains 743,867 bytes and retains every reported original
subject/view, result, independent assessment and source correspondence. Indexed
queries retain their complete order; digit allocation views retain their counter;
original assessment qualities are computed from the actual returned assessment.

Comparison took 5.92 s and presentation
3.02 s under the combined schedule. The three-recipe
cycle passed in 152.10 s with 1,624 theories and both host suites. Earlier cold
and JSON counts above are historical. See [the measurements](generation-presentation-measurements.json).
Accepted pairing permits retirement of the old stage; it does not establish
native-driven development or broader workflow adequacy.
