# Persistent digit histories with original policy conditions

A closed history now stores its fixed source and policy header, actual digit
material, ordered ledger and exact membership cache. The original finite
history state is derived from those components. Loading recovers every original
field, the complete ledger relation and the actual allocation head.

Actual indexed membership gates constructor-parametric replay and policy
checking. A complete generation backend contract owns readiness, generation
identity, its original reading, old-material agreement and exact predecessor
sites. Original formation and inclusion follow from these prerequisites.
The digit constructor instantiates that contract. Complete optional projection
then preserves the original bounded attempt, append and closed transition.

The policy checker computes one complete generation scope family. Certification
and policy alignment retain their original separate existential witnesses.
Complete scope equations connect the actual digit operation to both original
conditions. Optional filtering, whole-result views and subsequent-request
iteration are reusable contracts, including every refused or unavailable
position. Subsequent requests use each returned state and its ordered ledger.

Nineteen actual methods run on sixteen subjects. Each observation compares the
whole allocation head, original state and membership relation under the
original transition condition. Coverage also requires an actual successful
original bounded transition. The typed digit operation and bounded reference
are selected and adequate. The legacy original operation remains a separate
candidate; its different successful allocations fail this complete bounded
operation comparison.

Controls expose stale or absent caches, omitted or truncated ledgers, reordered
members, missing membership or policy checks, omitted replay checks, changed
requirements, refusal, a no-op, missing old material, deleted result families
and conflated unavailable input. Omitting membership admits case 3, although
its requested generation was not admitted into the ledger. Omitting policy
admits cases 7 and 8; omitting replay and policy also admits cases 5 and 6.
Deleting the whole family passes soundness and fails completeness on every
case. Conflated unavailable inputs fail cases 9 and 10.

A stale cache preserves the whole original result state while losing the new
member. A further actual boundary-only graft reservation preserves both that
state and its member relation while advancing the head once more. Both fail
complete result comparison. Neither cache fidelity nor original-state equality
alone establishes the whole required transition.

All twelve original source families are compared completely with the earlier
accepted history execution. Four additional inputs follow one, two, three and
four actual digit transitions. After four steps, the next input has head 9,
four ledger members, sixteen artifacts and eighteen bindings. Its successful
result has head 11, five ledger members, twenty artifacts and twenty-five
bindings. All sixteen independently computed bounded input projections pass
native equality. Finite-set enumeration order remains visible in the reports
and does not replace relation identity.

The three initial selections have 205, 18 and zero residual distinctions,
with 1,822, 191 and zero repairs. Every revision selects both facets and leaves
no scoped residual. Every original source, prepared operation, candidate value,
comparison and revision reason is included in the 54 complete native reports.

```sh
python3 -B tools/reconstruct_digit_history.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/digit-history-reconstruction
```

All 690 theories were rebuilt from HOL. Every one of the 54 complete parsed
reports matches the preceding execution. All original and database source
bytes, runtime inputs and archived inputs were verified. The reconstructible
source boundary contains 715 files and retains the complete native result
comparison without storing its reproducible report bulk in the repository.

This construction and comparison do not establish complete historical
permission or reachability. Full physical cost, broader workflow adoption,
all six `problems.txt` conditions, native mathematical-proof admission,
genesis and the final repository audit remain open.


## Native presented report (2026-09-17)

After accepted paired validation, this recipe now retains only its native word:
418,306 bytes, one complete report record. Every prior subject and
field remains in that presentation, with stores identified through their original
views and each distinct artifact given once. The word boundary is unchanged.
The current presentation took 228.95 s under the combined schedule.
See [the current measurements](replay-presentation-measurements.json) and
[current verification](current-verified.json). The earlier source-only counts and
JSON report counts above describe historical boundaries.
