# Complete replay over persistent digit material

The replay operation uses the actual digit generation constructor and preserves
the complete independently established bounded replay result. Literal replay
admission, actual judgment quotation, predecessor rows, allocation, generation,
material and all refusal cases remain part of that operation.

The original certified-cause reader supplies separate diagnostics for each
complete returned value. Generation, payload agreement and certification remain
distinct from equality to the requested operation. The structural identity
representations preserve complete value equality and the original finite reader
conditions. They do not supply satisfaction through a host digest or a table.

Eighteen methods run on all twenty-four subjects. The typed operation and bounded
reference, methods 0 and 1, are selected and adequate. The original allocator,
omitted replay admission, alternate quotation constructions, changed outputs,
empty results and dropped input families remain explicit controls.

The omitted replay guard produces additional uncertified successes on cases 1,
2, 3, 12 and 13. Actual scope alignment additionally distinguishes cases 2 and
12. Removing the quotation loses all scope readings; an enlarged quotation
fails certification. A reordered quotation can retain certification while
failing canonical quotation and the complete result comparison. Changed heads,
judgment fields or quotation outputs can likewise preserve weaker cause
properties while failing the required operation result.

The original non-byte-valued use-word case 6 remains present and successful,
with a certified original cause. Its complete quotation is particularly large.
No byte restriction or reduced input family is introduced to make it cheaper.

All seven original literal inputs, twelve original history inputs and the
generation input retain complete source correspondence. Subsequent inputs use
the actual returned material after one, two, four and eight steps. Their next
successful heads are 5, 7, 11 and 19. A failed load, an unavailable input, an
available refusal, an empty result and a dropped input family remain separate.
The dropped family passes soundness and fails completeness on every subject.

Every native source projection is equal to its original input. Finite-set
enumeration order can differ; the complete native relation equations establish
the required identity. The initial selections `[]`, `[0]` and `[0,1]` are formed
and have 135, 16 and zero residual comparisons, with 2,440, 225 and zero repair
witnesses. Every revision uses both facets and every followed basis is formed
with no residual.

The complete execution contains 512 reports: all contexts, every candidate
value and cause reading, all native observations, comparisons, revisions and
source correspondences. Separate context and candidate records preserve every
field. Verified compression retains the entire 6,772,393,096-byte stream; the
complete shared parser reproduces its original canonical boundary. Earlier
partial runs remain incomplete evidence, with their full unfinished bytes
retained separately.

The source-only boundary contains 691 theories, 38 Python files and the expected
complete report boundary. It includes the original history-append dependency;
the temporary cost-profile export remains a diagnostic artifact.

The fresh reconstruction rebuilds all 691 theories from HOL without a
reused parent context. All four stages succeed and every complete parsed
report matches the earlier 512-report execution directly. All
730 source-boundary files and 6 recipe inputs are verified,
along with all 724 earlier and 726 reconstructed
execution inputs and their archived originals. The complete retained archive
replaces 925,117,156 bytes of generated proof, database,
diagnostics and native execution copies.

The full prospective 1,442-theory integration check and all 102 host tests
with 59 subtests pass. All forty-one preceding complete source closures,
toolchains and original verification records remain unchanged.

The large complete output and long native execution expose remaining physical
cost. Structural identity and shared readers preserve the original meaning;
they do not establish a bound for the whole operation or reporting process.
Broader workflow adoption, all six conditions in `problems.txt`, historical
permission and reachability, native mathematical-proof admission, genesis and
the final repository audit remain open.

## Native presented report (2026-09-17)

The current recipe uses only its complete native word: 20,519,284 bytes, with
unchanged SHA-256 `3bb5958241e74fd03e43412abb5073a5ffef2cd98073f724bbf4207530ccc36b`.
Actual candidate results supply complete cause targets for shared computation;
independent target reads run through the existing parallel map. Every original
cause assessment still computes its own generation, payload and policy fields.
The word encoder retains complete objects for lookup and converts each distinct
artifact to rows once, preserving exactly the old word.

Complete probe words were compared byte for byte. The actual recipe passed in
279.14 s and its affected validation cycle in 365.58 s, with both host suites
passing. See [cause measurements](prepared-cause-measurements.json) and
[word measurements](object-word-measurements.json). Earlier timings, cold-source
counts and JSON counts above are historical. Broader adequacy and native-driven
refinement selection remain open.
