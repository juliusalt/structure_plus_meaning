# Exact encoded environment stores

The complete original artifact and binding relations determine the lookup
contract independently of a key representation. Any injective use and slot
encodings preserve the complete rows and local insertion equations. Exact
complete decoders recover the whole environment. The original local guards and
formation proofs transfer through those lookups; the reference index used in
the proof is not constructed during a local update. Unary and digit instances
implement the independent original guarded constructor on every input.

The native candidate has two actual components: its raw index view and its
guarded update. Soundness and completeness of the update are distinct from
preserving all original raw rows. The first twenty-input run admitted a
slot-clamping candidate under the update-only conditions. That result did not
answer the original arbitrary-row representation requirement. An additional
exact observation now compares the whole raw view against the original input,
and two new subjects retain malformed slots `[256]` alongside `[0]`.

All forty-nine previous complete reports are recovered unchanged from the
expanded execution: whole subjects and results, original conditions, comparison,
repair cycles and complete counted insertions. The expanded sixteen-method,
twenty-two-subject family produces fifty-nine complete reports. Its exact typed
original, raw original, general unary and general digit methods are selected
and adequate. The slot-clamping method preserves the update refusals but changes
an actual raw `[256]` slot into `[0]`, failing the new independent condition.
The previous update-only comparison and every original repair remain available.

Starting with only update conditions leaves sixteen residual pairs and fifty-two
repair witnesses. The native revision adds raw representation and leaves no
residual. From no facets, 164 residual pairs and 1588 repair witnesses lead to
`[1,2,0]`; every witness and resulting observation is reconstructed.

Actual relation insertions preserve the same complete result environments.
For original keys `[1]`, `[2]`, `[33]` and `[129]`, each unary read/update traversal
has 4, 5, 36 and 132 steps; the corresponding digit traversal has 5, 7, 15 and 19.
The reports retain full keys, original optional buckets, both insertion counts,
updated buckets and whole updated environments. Small-key overhead remains
visible. These supplied-key insertions are not yet cached allocation histories.

Finite candidate and scope coverage, cached digit allocation, indexed graft and
generation adoption, arithmetic and full physical decision cost, workflow and
genesis remain open. Raw representation does not admit an unformed transition.
Run `python3 -B tools/reconstruct_encoded_environments.py --poly <PolyML> --output <fresh>`.
The complete source boundary rebuilds every proof, export and report while the
repository retains only the reconstructible boundary.
