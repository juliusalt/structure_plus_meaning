# Native generation records

The original history, locus, payload, cause and complete predecessor requests
precede every candidate. Each predecessor request retains its actual use,
address and generation core. The guarded constructor checks these original
premises before extending the environment.

Thirteen actual methods run on sixteen complete subjects. Their 1,872 condition
positions cover availability and refusal, the four recovered core fields,
environment formation, actual native generation reading, preservation of every
old artifact and outgoing binding, and exact original predecessor references.
All actual environments, recursive cores, field readings, references and
comparison reasons remain in the 37-report reconstruction boundary.

Native selection and adequacy retain methods 0 and 1: the original constructor
and its reversal of the predecessor presentation. Separate theorems prove both
methods correct on every complete input. No particular predecessor order is
required by the original conditions.

Case 13 isolates two distinctions. Method 11 redirects a predecessor to an
equal generation at another existing use. Every condition except original
reference identity still holds. Method 12 removes unrelated old material and
its incident bindings; the new environment and native generation still pass,
but preservation of the old environment fails. A comparison omitting these
conditions retains unresolved distinctions; its computed revision adds them.

```sh
python3 -B tools/reconstruct_generation_records.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/generation-record-reconstruction
```

The execution includes whole targets and an invalid anchor. Successful
anchored-target fixtures remain a coverage limitation of this execution;
the constructor theorems cover both original target forms. Generation formation
does not establish cause truth or historical permission.

`finite_record_native_replay` also has an all-input theorem connecting every
successful joined result to the original certified-base-cause relation. It
requires an actual closed replay for the same whole payload, quotes its least
judgment environment, and preserves the original history. The executions here
compare record construction and the [literal-replay gate](literal-replay.md).
Execution of that complete joined constructor and arbitrary cause-value
reading remain further work. Whole workflow transitions, original problem and
criticism coverage, policy and history permission, physical cost and genesis
remain open; this component does not close `problems.txt`.
