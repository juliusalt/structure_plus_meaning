# Indexed generation reading

The original generation reader, recursive checker, record-readiness predicate
and anchor selection now have actual implementations over artifact and binding
lookups. Generic citation contracts cover every original citation form, target,
location and anchored value. A formed environment supplies their complete
original meaning; the existing closed digit-store type carries that invariant.
Individual reading does not reconstruct the whole old environment or repeat
its formation check.

Initialization enumerates the complete original finite environment once. It
preserves every artifact and binding and the computed initial allocation bound.
Malformed input stays unavailable. The actual enumeration order of finite-set
fields can differ from the original listing. Their exact relation equality is
proved for every input and computed natively in each source comparison. Every
complete source before loading is also retained; enumeration identity is not
silently substituted for equality of the original finite relations.

Fifteen actual methods run on thirty-two subjects. Each result retains the
whole readiness value, optional selected anchors and ordered query results.
Every query preserves its complete site and generation claim, all generation
field readings, the recursive-check result and the optional complete anchor
artifact. Shared preparation computes actual variants once; its equation
connects every assessment to the original complete method.

Both the typed reader and complete original reference are selected and
adequate. Controls expose missing bindings, changed query coordinates, forced
checks or readiness, shallow top-field matching, lost anchors, field readings
or query results, empty output families and conflated unavailable input.
Ten cases reject a shallow checker that accepts a changed predecessor while
the complete top field readings remain unchanged. Duplicate equal generation
claims at different uses remain readable but fail original record readiness.
Wrong generation claims can have valid field readings and still fail checking.

All sixteen original construction requests are retained. The other sixteen
subjects query actual successful original outputs, or retain the original
failed request. All thirty-two complete pre-loading sources agree directly
with those earlier original inputs or outputs. Native complete projection
equality holds for every one. The malformed input is unavailable in cases 11
and 27; it is not replaced by a false-valued report.

```sh
python3 -B tools/reconstruct_indexed_generation.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/indexed-generation-reconstruction
```

The complete 168-theory closure was rebuilt from HOL. All 102 complete reports
match the preceding native execution, with every source, database source,
execution input and archived input verified. The three initial selections have
130, 11 and zero residual distinctions and 3,020, 170 and zero repairs. Native
revision selects both original facets and leaves no scoped residual.

Complete generation installation, full physical cost, all six `problems.txt`
conditions, complete historical permission and reachability, native
mathematical-proof admission, genesis and the final audit remain open.
