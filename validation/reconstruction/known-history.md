# History construction from admitted predecessor readings

The closed history already establishes the original generation reading for
every row in its admission ledger. Its exact index checks each complete
requested row at its original site. The new constructor uses those established
readings instead of recursively checking them again.

`generation_record_targets_ready` retains target formation and predecessor
distinctness. The factored body still queries the actual anchors, allocates at
the stored head, installs every predecessor binding and grafts every literal.
The constructor equation requires all original predecessor readings. It does
not admit arbitrary rows merely because their targets are formed.

The public closed history step supplies that premise through the original
state invariant and actual indexed membership. Replay, quotation and the whole
original digit policy check remain executable. Complete optional equality
preserves refusal and every successful field: material, allocation head, fixed
header, ordered ledger and membership cache. The theorem establishes equality of the entire closed result.

The native comparison contains twenty methods on all sixteen original
subjects. Method 0 uses the established predecessor readings. Methods 1–19
retain the nineteen original operations at successor indices. Every original
context, complete method result, source field and native inspection matches
the preceding history execution. The new method's complete values equal the
original typed method on all sixteen subjects.

The native selected and adequate methods are 0, 1 and 2: the new operation, the
original digit operation and the bounded reference. All seventeen controls
remain distinguishable. Missing membership fails case 3; missing policy fails
cases 7 and 8; omitting replay and policy additionally fails cases 5 and 6.
Dropped families remain sound but incomplete. Unavailable input stays distinct
from an available refused request. Stale caches and an extra real reservation
remain wrong complete results even where weaker state properties agree.

Successful outputs for the four subsequent inputs retain heads 5, 7, 9 and 11
and complete caches with two, three, four and five members. All twelve original
sources and all four subsequent inputs retain exact native correspondence.
These inputs are the original persistent states; the all-input equality of
the closed step establishes the corresponding transition equation.

The revision review also exposed a configuration error. The final supplied
selection `[0,1,2]` used method indices where the cycle expects observation
facets. Available facets are `[0,1]`. The native initial report rejects that
selection as unformed despite its empty comparison residual. Revision removes
unavailable facet 2 and recomputes a formed basis on `[0,1]` with no residual.
The input error and its repair are retained explicitly. This withdrawal is a
domain rejection, not a claimed observation conflict.

The empty and single-facet selections have 222 and 19 residual comparisons and
2,032 and 207 repair witnesses. All three native revisions return `[0,1]`, and
every followed report is formed with no residual comparison.

The complete native boundary contains 374 reports, including all candidate
values and every revision reason. Verified compression preserves their full
byte stream, and separate context and candidate records permit complete review
without accumulating the entire execution in memory. The source-only boundary
contains 697 theories, 29 Python files and the expected report boundary.

The fresh reconstruction rebuilds all 697 theories from HOL with no reused
parent theories. All four stages succeed, and every one of the 374 complete
parsed reports equals the warm execution directly. All 727 source-boundary
files, six recipe inputs and both executions' 723 current and archived inputs
are verified. The complete retained archive replaces 851,282,976 bytes of
generated proof, database, diagnostic and execution copies.

The full 1,406-theory integration check and all 102 host tests with 59 subtests
pass. The forty preceding source closures and reconstruction records remain
unchanged.

The refinement removes a repeated recursive check. It does not establish a
full physical cost bound: membership equality, distinctness, quotation, replay,
policy, new material and reporting retain their own costs. Original history
validity also remains distinct from complete historical permission and
reachability. Broader workflow adoption, all six conditions in `problems.txt`,
native mathematical-proof admission, genesis and the final audit remain open.


## Shared presented report (2026-09-17)

The current recipe has two accepted stages on the same export: the original
complete comparison and a native presented report word of 449,531
bytes. The word includes each distinct complete artifact once and identifies
every presented term and every store through its original view. The combined
recipe retains 375 records. See [the measurement record](presented-history-measurements.json)
for source identities, word digests and timing boundaries. This incremental
validation reused the fixed accepted base; the earlier cold-build counts above
are historical, not a description of this run.
