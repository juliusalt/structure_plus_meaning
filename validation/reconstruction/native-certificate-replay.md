# Reconstruct complete native certificate replay

The complete original native source, proof and required call determine whether
replay construction is available. Every certificate node keeps its whole proof
and call. The constructor positions inference metadata at the actual source
owner, obtains coordinates from original paths, and installs the complete graph
through an explicit injective correspondence. Different proofs or calls remain
distinct; two indexed edges may still share their original endpoint.

An actual native application is constructed for the original call. The full
extending environment preserves every earlier artifact and outgoing binding.
The separate least replay environment retains the source, application and graph,
and the ordinary native replay reader returns the empty assertion boundary.
These are distinct returned values with complete correspondence contracts.

The ten independent conditions cover actual graph recovery, complete original
graph mapping, source preservation, freshness, injectivity, the original source
and call, exact least retention, retained recovery, closed replay and refusal
when the original source or certificate is unavailable. One universal constructor
theorem establishes these conditions from the original source and certificate.
Direct graph placement and replay use the same graph-assessment contract.

The execution includes seven families and 28 original certificates, including
the actual identity and shared-child source from the earlier scope repair.
Fourteen methods produce 392 individual results and 980 family condition
positions. The comparison selects only method 0. Erasing a claimed map or graph,
changing the root or call, removing installed artifacts, and altering the
retained environment preserve their complete native failure reasons. The input
with an unused original artifact distinguishes extending source preservation
from least replay retention: the extending environment keeps that artifact;
the replay environment omits it.

Closed replay alone does not certify every claimed result field. Some altered
maps, graphs or application interiors still accompany a replayable retained
environment; their separate correspondence or call conditions reject them.
The complete bodies, source and graph readings, all individual and family
conditions, comparison profiles, directed losses and three revision cycles were
inspected. Each followed revision is formed with no remaining comparison loss.

`Finite_Evaluation_Caches` factors repeated actual reads by their complete inputs.
The five replay caches share package, graph, application, retained-boundary and
closed-replay readings. Cached assessment equals the full original assessment
for every candidate, including queries absent from the prepared cache. No hash
or ordering on proof values determines equality. The two complete 19-report
executions agree in every field and order. Their observed runtimes were about
1,121 and 202 seconds under concurrent work; this is a measured run improvement,
not a complete physical cost bound.

~~~sh
python3 -B tools/reconstruct_native_certificate_replay.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/native-certificate-replay-reconstruction
~~~

The recipe retains the complete source and expected 19-report boundaries.
Materialization and reconstruction use the shared runner to rebuild the entire
closure from HOL, inspect diagnostics, export accepted code and execute every
original field. The host serializers add no satisfaction judgment. Generated
proof snapshots, code and full reports are reproducible outputs.

The preserved certificate placement and replay work is complete at this scoped
boundary. Arbitrary development-subject coverage, operative whole-workflow
admission, historical permission, the full cost account, native mathematical
proof admission and genesis remain open. The six conditions in `problems.txt`
remain the immediate development milestone.

The source boundary contains 548 files: 522 theories, 25 Python
modules and the expected-report comparison. A fresh materialization rebuilt
the entire proof closure from HOL and reproduced all 19 complete reports.
The [current verification](current-verified.json) records the complete
1,142-theory build, passing host tests and twenty source-only boundaries for
1,213 complete reports. Three changed recipes were freshly reconstructed;
seventeen unchanged complete input boundaries retain their earlier verification.
Generated copies were removed after source, diagnostic and full-report review.
