# Reconstruct certificate paths and scoped development admission

Complete native sources and requests supply the original program, answered calls
and certificate families. Each node keeps its whole certificate and required
call. Indexed child relations determine every path and endpoint. The constructor
uses actual path representatives, preserves the complete original graph through
its returned map and retains genuine shared endpoints. It does not require an
order on certificate values.

The seven independent conditions require exact answers and source, a complete
functional result family, every original path, complete graph correspondence,
coordinates that actually reach their stated original node, and refusal when the
original input is unavailable. The constructor satisfies them for every original
family. The same assessment and constructor contracts serve explicit earlier
queries and supplied certificate families. Supplied families are available only
when the actual native source is recovered, their stated positive calls are
covered and every complete certificate passes the independent original checker.
Several certificates for one call remain distinct.

The initial 26-input comparison evaluates sixteen actual methods. Methods 0, 12
and 13 are selected and adequate on that scope. Complete original-scope criticism
finds no same-proof/different-call, same-call/different-proof or shared-path
witness. The shared development cycle therefore admits no method. Executing a
comparison alone cannot bypass its computed criticism.

The scope repair compares retaining all original inputs, removing them, and
appending four actual source/certificate inputs. Its retention condition concerns
complete raw inputs, including invalid and unavailable requests. The added source
has two equal leaf definitions with two clauses each, a parent requiring one leaf
twice, and a parent requiring the two definitions. The existing native proof
constructor generates all supported child combinations. The supplied copy has
twelve accepted certificates. An omitted-answer variant has valid certificates
but uncovered stated answers; a corrupted-root variant has covered answers but
twelve rejected certificates. Both invalid supplied variants remain unavailable.

The expanded scope alone satisfies all four retention and coverage conditions.
Every conflict retains both complete original nodes, their common key and actual
source paths. The complete comparison and all three revision cycles select this
scope. Only after inspecting those results was the shared development cycle
executed on it.

The 30-input cycle preserves every earlier input, candidate output and assessment.
It evaluates 3,360 condition positions: 2,999 hold and 361 fail. Methods 12 and 13
now fail the actual-path condition on both new valid inputs. Their returned maps
still describe their altered graphs; those maps choose coordinates reaching a
different original node. Method 0 preserves the full identities and genuine
sharing. The full comparison selects method 0, computed criticism accepts the
scope and the shared admission operation returns only method 0.

Every complete source, candidate result, inspection, original witness, comparison
profile, directed loss and revision field was inspected. Repeated serialized rows
remain present. Exact correspondence to earlier complete values permits their
reuse without dropping any result field. The host serializers and contract reader
supply no satisfaction table or semantic verdict.

Five recipes retain this complete sequence:

| Recipe | Reports | Original subject |
|---|---:|---|
| `reconstruct_native_certificates.py` | 57 | Initial complete certificate construction comparison. |
| `reconstruct_certificate_coverage.py` | 7 | Initial scope and its missing distinctions. |
| `reconstruct_certificate_development.py` | 57 | Initial computed criticism and empty admission. |
| `reconstruct_certificate_scope_repair.py` | 7 | Complete original inputs and actual scope correction. |
| `reconstruct_certificate_input_development.py` | 65 | Expanded cycle, proof checks and actual admission. |

Each recipe uses the shared reconstruction runner. For example:

~~~sh
python3 -B tools/reconstruct_certificate_input_development.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/certificate-input-reconstruction
~~~

The corresponding `*-sources.json` files retain the complete theory and host-tool
source closure and the `*-reports.json` files identify complete expected
reproduction. Materialization copies only those repository inputs. Running without
`--proof` rebuilds the contracts from HOL, retains complete diagnostics, exports
proved code and reproduces every report. Generated proof snapshots, native code
and complete result logs are reproducible bulk, rather than retained repository
inputs.

The [current verification](current-verified.json) records the complete
1,180-theory build, passing host tests and twenty-two source-only boundaries
for 1,270 complete reports. All recipes were freshly reconstructed through
one source proof of their combined dependencies and their original executions.

This is a scoped development admission construction. The six-condition milestone
in `problems.txt` remains open: the complete development workflow still needs its
native Factor presentation, admission and enforcement, adequate problem and
candidate generation and criticism, historical permission, and a complete account
of decision cost independent of accumulated process. [Source positioning, native certificate placement and replay](native-certificate-replay.md)
have a separate complete reconstruction. Native
mathematical-proof admission, genesis and the final repository audit also remain.

## Native presentation (2026-09-17)

The paired transition was accepted in `760dcf5`. This recipe now retains only
its native word: 890,378 bytes, one complete report record. The
boundary is unchanged and all original subjects, results, readings, inspections
and revisions remain in it. The retirement stage passed in 6.53 s;
the combined four-recipe validation took 106.14 s with both host suites passing.

See [retirement measurements](native-retirement-measurements.json) and the
[preceding paired evidence](native-presentation-cycle-measurements.json).
Earlier source-only and JSON counts above describe historical boundaries.
