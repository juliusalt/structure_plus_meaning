# Reconstruct source-derived certificates

The independently stated problem is constructing complete valid certificates
for exactly the positive calls in the original finite request. The complete
native environment, package root and requested call family remain the input.
The actual source reader supplies every clause, binding and premise socket.
Unavailable source, head-coverage or demand-closure prerequisites return no
result; an available empty answer remains a returned result.

The constructor follows the original progressive inference history. Each
activated application selects complete functional child families from earlier
certificates. A shared history-fold argument proves that the certificate
claims project exactly to the existing evaluator and that every certificate
passes the original proof checker. A finite sufficient family need not
enumerate all proofs of a cyclic program.

Finite terms instantiate the original recursive proof datatype. The independent
finite checker recovers the actual admitted instance from the original program,
checks complete bindings and material conditions, and recursively inspects the
child at every required socket. Its exactness theorem covers every finite
input, including malformed child families. Complete inspection rows retain
each certificate and its actual checking result.

The investigation computes six independent original-subject conditions:

1. Every returned answer is an original requested positive call.
2. Every original requested positive call is returned.
3. The original source program is retained.
4. Every offered certificate passes the original program's proof checker.
5. The complete certificate claim domain equals the returned answer family.
6. Unavailable original prerequisites produce no result.

The first five conditions require a result when the original source and
evaluation are available. The complete constructor satisfies all six conditions
universally. Candidate indices identify actual operations, workload indices
identify complete original native inputs, and the observation equation connects
each computed inspection to its independently stated condition.

The first execution compared eleven methods on the existing twenty-six history
problems. It selected only the complete constructor. Complete counterexamples
show that an identical certificate can pass in one original program and fail in
another. An identical empty returned result can also be valid for an available
empty source and violate refusal for an unavailable source. Neither a proof
value nor a returned result determines its assessment without the original
context.

Criticism exposed three untested defects: invalid non-root children under an
intact root, formed wrong binding values under unchanged keys, and valid extra
certificates with unchanged answers. Four additional operations corrupt immediate
children, add conflicting children under the same sockets, replace binding
values by the formed payload [255], and return the actual expanded certificate
family with the original answers and program. Shared native contexts compute
the original and expanded families, and a complete packet equation preserves
each original candidate, assessment, evidence and comparison.

The expanded execution retains every field of all 286 original cells.
Its fifteen methods and six conditions give 2,340 positions; 2,007 hold.
Twenty-two original inputs are available and four require refusal. Only method
0 is selected and adequate. The two child controls fail certificate validity
on six inputs; the wrong-value control fails it on twenty. The extra-certificate
control retains valid proofs and exact answers while failing domain equality
on twenty-one inputs, including the nonempty request with an empty answer.
Its complete extra-claim families are retained.

All four complete revision cycles are retained and inspected. Starting with
no conditions leaves 162 comparisons and 3,808 repair witnesses; the returned
selection is [4,2,1,0,5,3]. Starting from answer soundness, source identity and
refusal ([0,2,5]) leaves 96 comparisons and 1,485 witnesses; the revision adds
[4,1,3]. Both followed comparisons have no residual. The full basis and the
first execution's returned basis [5,3,4,2,1,0] remain sufficient. These finite
results establish no minimum-cardinality or arbitrary-method claim.

The complete report has 58 records: 26 complete source/candidate subjects,
26 complete assessment and evidence families, the scope, the comparison and
four revision cycles. Every recursive proof, binding value, material operand,
source artifact, requested call and claim difference is reconstructible.
The host launcher serializes actual native outputs and checks their complete
reproduction; it supplies no semantic observations.

With Isabelle and its matching Poly/ML executable, reconstruct in a fresh
output directory:

~~~sh
python3 -B tools/reconstruct_native_derivations.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/native-derivation-reconstruction
~~~

The [source boundary](native-derivations-sources.json) retains 492 files:
469 theories, 22 Python modules and the
[complete-report comparison](native-derivations-reports.json).
The [verified reconstruction](native-derivations-verified.json) rebuilt that
closure from HOL, collected full proof diagnostics with no errors, exported
the proved code and reproduced every report field. Every original source and
execution input was verified. The
[materialization receipt](native-derivations-materialization.json) identifies
the exact collected inputs. The full 1,180-theory repository check and all
98 host-tool tests passed.

Recursive explanation traces for every failed checker subcondition remain a
separate requirement. The complete original program and counterexample trees
are present. Their local clause, binder and socket addresses still require
actual source positioning before graph placement. Native proof-artifact
construction and replay, native mathematical-proof checking, complete native
candidate generation and criticism, development-cycle admission and enforcement,
the full physical cost account, genesis and the final line-by-line audit remain
open. The investigation's own result does not authorize its handoff.
