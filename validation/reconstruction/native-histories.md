# Reconstruct histories of actual native inference

The original problem requires exact positive answers within the actual demand,
the original source, refusal when the evaluator prerequisites are unavailable,
every progressive preceding state, and the complete activated clause, binding
and indexed premise family. The finite history constructor starts from the
empty seed family. Its projection is exactly the existing evaluator, including
unavailable inputs. Every retained activation uses the original admitted schema
instance and its actual source coordinates.

The reusable iteration contract retains every preceding state and final stop
condition. The inference instance retains all enabled original witnesses at
each state. Separate reviews retain every expected and offered occurrence and
every extra or missing witness. The observation equation connects the actual
history operation and complete source problem to the six independent conditions.
Answer agreement alone does not discharge the evidence requirements.

The original comparison executed nine methods on 21 native inputs. Only the
original constructor satisfied every condition. The other methods erase or
reverse the state sequence, erase bindings or premise families, return every
requested call, withhold available results, replace the claimed source, or
fabricate a result when a source is unavailable. All 21 original environments,
programs, demands, application families and evaluator results equal the earlier
native evaluation reports.

Without any conditions the first scope leaves 55 comparisons unresolved and
returns 1300 repair witnesses. Starting from answer soundness, completeness,
source identity and refusal leaves 25 comparisons and 280 witnesses. The first
revision returns `[3,0,1,4,5,2]`; the second adds the missing state and witness
conditions. Both followed selections resolve every residual. The full six
conditions remain unchanged. These returned selections are not claimed minimal.

A sharing proposal constructs the complete contexts and assessment cells in
HOL and reuses those same cells for reporting and comparison. Its theorem
preserves every original subject, assessment, ordered observation, comparison
and revision field. The original unshared execution timed out after 1800
seconds with 38 complete reports. The shared execution completed all 47 reports
in about 148 seconds and matched that entire retained prefix. Those durations
are execution records, not a complete cost account.

Independent criticism adds true answers outside the requested demand, refusal
because of an unrequested uncovered head, and actual compiled inputs with
missing head coverage and an open demand. The first expanded run retains all
189 original assessment cells unchanged and again selects only the original
constructor. Its 56 reports replay the original returned basis without a
residual. This execution exposed a fixture description error: the closed demand
in case 23 has two positive answers in the actual source. That case is retained;
a further demand requests only the original equality guard at an empty payload.
The native execution returns an available empty answer and empty history for
that nonempty demand. The incorrect all-demand and expanded-demand methods
both fail its answer and state conditions. Every earlier subject and assessment
remains unchanged.

The final scope has eleven methods, 26 actual problems and six conditions.
There are 1444 satisfied observations among 1716 positions. Twenty-two problems
are available; the two unavailable sources, uncovered requested head and open
demand remain unavailable. Only the original constructor is selected and
adequate. Starting without conditions leaves 87 residual comparisons and 2192
repair witnesses; the revision returns `[1,5,2,3,0,4]`. Starting with the first
four conditions leaves 32 comparisons and 393 witnesses; the revision adds
the state and witness checks. Every followed selection has no residual.
Both the full initial selection and the original returned basis remain unchanged.
All conditions are safe in this scope, with no conflicts or unrepairable pairs.

The complete report boundary contains 58 reports:

| Family | Reports |
|---|---:|
| Actual source problems and candidate histories | 26 |
| Full assessments and structural evidence reviews | 26 |
| Scope | 1 |
| Observations, comparison, selection and adequacy | 1 |
| Complete initial, repair, revision and followed cycles | 4 |

Use fresh directories and a fresh session:

```sh
python3 -B tools/materialize_source_boundary.py \
  --manifest validation/reconstruction/native-histories-sources.json \
  --output /tmp/native-history-inputs
python3 -B /tmp/native-history-inputs/tools/reconstruct_native_histories.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64-linux/poly \
  --session Native_History_Independent_Run \
  --timeout 1200 \
  --output /tmp/native-history-results
```

The [source boundary](native-histories-sources.json) contains 465 files: 444
theories, 20 Python modules and the [complete-report comparison](native-histories-reports.json).
The [verified reconstruction](native-histories-verified.json) rebuilt every
theory from HOL and reproduced every report field. The
[materialization receipt](native-histories-materialization.json) identifies its
exact input boundary. All original source bytes, actual execution inputs and
complete proof diagnostics were checked.

The full 1,025-theory repository check passed. Six affected earlier boundaries
also rebuilt their complete source closures from HOL and reproduced all 647
reports. Two unaffected boundaries retain their previously verified 41 reports
with identical complete inputs. The nine boundaries retain 746 reports in total.

Native proof-artifact construction, native mathematical-proof checking, complete
development-cycle enforcement, arbitrary-method coverage, the full cost account,
genesis and the final line-by-line repository audit remain open.
