# A computed native choice performs subsequent development

`native_steered_development` receives original comparison questions and
subsequent requests. It computes actual producer results and independent
original-goal references, then derives ordinary native clauses for all five
conditions. Exact clause membership and a separate observation equation connect
each criterion to the actual producer and every original question. The public
operation receives no satisfaction table.

Those sources enter the same closed development cycle. Admitted values map to
actual producers. A unique choice invokes that producer for subsequent
requests, and every resulting claim is checked against the new request's
original native admission. The finite prior scope grants no independent
permission to accept a later result.

The full eight-question scope selects producer 0. All eight subsequent whole
reports and admissions equal the previously checked cycle, including refusals
and duplicate results. With only the first question, producers 0 and 8 are both
adequate: truncation is indistinguishable on that single-answer input. The
native choice remains absent. The empty scope also refuses. Both retain all
eight subsequent requests without executing them. A comparison with no repair
residual does not establish a unique producer.

Generated positions and method identities remain separate. The full run
selects generated position 9, whose value identifies producer 0. Both native
revisions retain all witnesses and finish with formed observation bases and
no residual comparison. The full, empty and single-question executions contain
119, 15 and 27 complete records. Reencoding both full input lists reproduces
all 119 records of the full run directly. Four actual parallel-map sites occur
in the exported module, including independent contexts and subsequent requests.

A fresh 493-theory reconstruction from HOL, with no supplied project parent,
reproduces all 280 complete records. Every source, certificate, comparison,
revision and admission matches the reviewed warm execution. The original
proof passed before a diagnostic write hit the temporary storage quota;
the remaining stages were resumed after verifying every original input.
The failed launch and accepted recovery remain distinct in the verification
boundary. The complete repository build passes all 1,528 theories; the
host suite passes 135 tests and 59 subtests, with two existing optional
dependency skips.

Run `python -B tools/reconstruct_native_steering.py --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly --output /tmp/reconstruct-native-steering --threads 16`.

`tools/check_native_steering.py` accepts complete lists through `--questions`
and `--requests`, together with `--proof`, `--poly`, `--project` and a fresh
`--output`. The reconstruction exports
`/tmp/reconstruct-native-steering/export/native_steering.proof.json`.

The ten-producer library, finite one-step generation and original question
scope remain explicit. Broader problem and candidate discovery, global cost,
native mathematical-proof admission, historical permission and reachability,
genesis and the final repository audit remain open. Isabelle retains its
normative bootstrap role through genesis.

[Sources](native-steering-sources.json), [reports](native-steering-reports.json)
and [verification](native-steering-verified.json) retain the reconstruction boundary.
