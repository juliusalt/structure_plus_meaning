# Original requirements and complete workflow paths

The compiler constructs guards for all eight original workflow positions from
their native sources and goals. Admission retains the original requirements,
complete branch traces, ordered results, source answers and certificates.
Missing positions and substituted protocols cannot authorize a result.

Generation obtains its premise answers from the actual native source and
retains clause occurrences, bindings and identified premises. Equal result
values retain separate occurrences. The input-scope comparison preserves all
six original questions and adds formed whole/anchored targets, a malformed
target and nested pair/collection goals. Scope selection uses their actual
structures. Both complete JSON input roundtrips preserve every field.

The source-only recipe reproduces 342 complete records. The declared native
goals still need adequate scope for their intended problem. The
[closed cycle](native-development.md) performs actual development operations;
[self-development](native-steering.md) uses its computed choice for later requests.

Run `python -B tools/reconstruct_native_workflow.py --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly --output /tmp/reconstruct-native-workflow --threads 16`.

[Sources](native-workflow-sources.json), [reports](native-workflow-reports.json)
and [verification](native-workflow-verified.json) retain the reconstruction boundary.
