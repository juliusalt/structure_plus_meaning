# Policy construction from the actual quoted scope

Successful original replay establishes a generation, a certified base cause
and its complete uniquely quoted judgment scope. The new policy operation uses
those established facts and computes the remaining actual package and policy
alignment check on that scope before constructing the generation.

A reusable optional-result theorem permits this ordering because every actual
successful output has the same check value. The complete optional operation
equals the original policy-filtered replay, including refusals and every result
field. The ordinary digit constructor instantiates the generation backend.
Original indexed membership and the complete history append preserve the
closed history result, including material, head, fixed header, ordered ledger
and exact membership cache.

This refinement is independent of the known-predecessor constructor. Its
ordinary constructor still checks predecessor readings. The separate
[composite construction](constructed-history.md) combines the two established
local premises. A full physical cost bound remains further work.

Twenty-one methods run on all sixteen original subjects. The new operation,
original digit operation and bounded reference, methods 0, 2 and 3, are selected
and adequate. All nineteen original methods retain their complete values and
native observations at indices increased by two. Every original source field
and each subsequent input remains identical to the preceding history execution.

The second added method omits literal replay admission while retaining actual
quotation, package and policy alignment, membership, construction and append.
It returns a raw candidate result. It cannot obtain an admitted closed history
through those weaker checks.

That control admits the changed-root case 6 and fails both native result
conditions there. Its complete output equals the earlier control that omitted
both replay and policy on that case. It still refuses the changed-payload case
5 because the retained alignment check observes that payload. This distinction
demonstrates why policy alignment cannot replace replay admission. The normal
operation retains the original replay guard.

All original controls remain observable: missing membership fails case 3,
missing policy fails cases 7 and 8, and missing replay and policy also fails
cases 5 and 6. The empty family remains sound and incomplete on every subject.
Unavailable inputs remain distinct from available refusals. Successful later
inputs retain heads 5, 7, 9 and 11 and complete caches of two through five members.

The generic additional-producer framework computes each added operation on the
actual subject and uses the original complete reference and coverage condition.
It assumes no producer correctness. The same exact observer equations therefore
cover the proved refinement and the failing control.

Initial facet selections `[]`, `[0]` and `[0,1]` are formed. Their residual
comparisons number 257, 20 and zero, with 2,272, 222 and zero repair witnesses.
All revisions select `[0,1]`, and every followed basis is formed without a
residual comparison. The complete native boundary contains 390 reports and
retains every candidate value and revision reason through lossless compression.

The source-only boundary contains 700 theories, 29 Python files and the expected
complete report boundary.

The fresh reconstruction rebuilds all 700 theories from HOL without a reused
parent context. All four stages succeed and every complete parsed report
matches the earlier 390-report execution directly. All 730 source-boundary
files, six recipe inputs and both executions' 726 current and archived inputs
are verified. The complete retained archive replaces 864,077,391 bytes of
generated proof, database, diagnostics and native execution copies.

The full prospective 1,442-theory integration check and all 102 host tests
with 59 subtests pass. All forty-one preceding complete source closures,
toolchains and original verification records remain unchanged.

The first host review requested the wrong serialized reference field and failed
before completing. Its unclosed report streams were stopped; the corrected
review used the actual `reference_readings` field, closed both streams on every
exit, and repeated the complete comparison. No native or proof input changed.

The successful comparison does not establish the whole physical cost account,
historical permission or reachability. Broader workflow adoption, all six
conditions in `problems.txt`, native mathematical-proof admission, genesis and
the final repository audit remain open.
