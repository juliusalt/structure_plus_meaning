# Original-policy history transitions

Preparation fixes the complete original source, requirement family, constructed
policy and called entry. The abstract history type carries original requirement
truth for every admitted payload and actual generation reading at its retained
site. A step checks every requested predecessor against the admission ledger,
uses the original joined replay and generation constructor, and checks the
resulting cause against the fixed original policy. The whole original request
remains unchanged, and previous material and admitted generations are preserved.

The native fixture obtains its actual certificates and replays from the existing
complete original requirement decision. It prepares an empty history and uses
the typed API to construct one or two preceding steps. A separate proved fixture
removes ledger entries while retaining valid material. Its failed step distinguishes
retention from admission; the fixture is not claimed reachable through the
preparation and step API. Duplicate predecessors, changed payloads or roots,
different original policies, unavailable preparation and absent replay material
retain distinct actual inputs.

Twelve methods compare complete optional state and certificate-family results.
The exact typed API and raw operation are selected and adequate. Controls omit
membership or policy, bypass replay and policy, refuse, retain an unchanged state,
delete the ledger, alter original goals, remove all certificate rows, add an
absent-key row, or confuse unavailable input with a rejected step. The added
family controls preserve every earlier complete subject and first-nine result
and condition. They distinguish soundness from completeness and force the
partial comparison to retain both observation facets.

The existing proved preparation and quotation-root code equations preserve all
original complete readings. Full state fields are streamed without joining an
entire report string. An absent input and a present rejected result retain
different representations.

```sh
python3 -B tools/reconstruct_required_history.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/required-history-reconstruction
```

All forty-one reports retain original states, requested steps, actual candidate
results, certificate keys and absence positions, computed conditions, comparison
and revision reasons, and coverage counts. Complete workflow-policy adequacy,
historical permission and reachability, indexed generation storage and allocation,
full physical cost, native mathematical-proof admission and genesis remain open.
