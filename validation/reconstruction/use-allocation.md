# Fresh-use mappings and coordinate growth

A common prefix-embedding contract preserves the supplied boundary at the
absent input, maps every present input outside all reserved uses and the
boundary, and preserves injectivity over the complete optional-use domain.
The original zero-prefix method and a compact one-coordinate method both
instantiate it. The compact coordinate is greater than every actual old and
boundary head; natural use coordinates have no byte bound.

The independent conditions inspect boundary identity, freshness, injectivity
and unchanged suffixes. A fifth condition requires one added coordinate.
Reusable complete function-graph observations compute the conditions from
actual mappings. Fourteen subjects and ten methods distinguish absent uses,
present empty words, long old words, coordinates above 255, and collisions
with a boundary that is absent from storage. The last two subjects and an
omitted-boundary method preserve every previous complete subject and first-nine
result and condition. A supplied boundary cannot be omitted from freshness.

Both exact methods are selected and adequate under the semantic conditions.
Only the compact method is selected and adequate when coordinate growth is
included. An initial semantic-only observation basis has six residual pairs;
the native revision adds the growth condition and leaves none. A stored word
of 128 coordinates makes the old method add 129 coordinates, while the compact
method adds one. This is a structural word-length result, not a bound on
natural-number bit cost or total decision time.

```sh
python3 -B tools/reconstruct_use_allocation.py \
  --poly /opt/isabelle/contrib/polyml-5.9.2-2/x86_64_32-linux/poly \
  --output /tmp/use-allocation-reconstruction
```

All thirty-five reports reproduce complete subjects, mappings, native coordinate
counts, conditions, both comparisons and every revision reason. The initial
maximum still scans old uses. Cached allocation, general graft and generation
transport, complete workflow enforcement, unrestricted scope coverage, native
mathematical-proof admission, full physical cost and genesis remain open.


The current index's natural component path has length n+1, as established by
`natural_binary_path_length`. A constant number of word coordinates therefore
does not bound its traversal. Shorter exact path encoding is a further problem.
